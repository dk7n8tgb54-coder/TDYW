# 2026-07-06 工作日志

## 运行日志统计概览（第一阶段轻量统计）

**任务**：基于现有 runlog 模块实现第一阶段数据分析功能，不改动核心业务逻辑。

**改动文件**（修改 3 + 新增 3，均只读统计，未触碰 models/Table/Form/store/exporters/pdf_export 等核心业务文件）：
- 新增 `spug_api/apps/runlog/statistics_service.py`：统计服务，纯数据库聚合查询
- 修改 `spug_api/apps/runlog/views.py`：末尾新增 `RunLogOverviewView`（只读，`@auth('runlog.runlog.view')` + 租户隔离）
- 修改 `spug_api/apps/runlog/urls.py`：新增 `overview/` 路由
- 新增 `spug_web/src/pages/runlog/Statistics.js`：统计概览页（KPI + bizcharts 趋势图 + 分布表 + 系统排行 + 未闭环列表 + 筛选栏）
- 新增 `spug_web/src/pages/runlog/statisticsStore.js`：统计概览 store
- 修改 `spug_web/src/routes.js`：运行日志改为父子菜单（事件列表 + 统计概览），复用 `runlog.runlog.view` 权限

**统计内容**：事件总数/今日新增/本月新增/未闭环/已归档/P0+P1 + 状态/级别/类型分布 + 系统排行 Top10 + 30 天趋势 + 未闭环事件列表，支持时间范围/类型/系统/级别/状态筛选。

**设计要点**：
- KPI 与未闭环列表基于非时间筛选查询集（反映当前态）；分布与趋势基于含时间范围查询集（默认最近 30 天）
- 全部 aggregate / values+annotate，不加载明细；trend 补齐空日期
- 复用项目已有 bizcharts（interference/upgrade 已在用），不引入新依赖
- 新增独立 `overview/` 接口，保留原有 `statistics/` 接口不动（Table.js 在用）

**验证**：Django check 通过；Docker 容器内 shell 调用统计服务返回结构正确；前端 lint 无错误。

**Docker 运行环境**：Docker 在 WSL 内运行，需用 `wsl docker exec ...` 调用；容器名 `tdyw-test`，项目路径 `/data/spug/spug_api`。PowerShell 无 `tail`，测试脚本输出重定向到文件再读取。
