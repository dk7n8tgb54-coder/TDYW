# 2026-08-02 工作日志

## 执照管理 + 合同协议 CRUD 审计修复

### 修复内容（5 项风险，28/28 测试通过）

**R-CA-3 (FIXED)**: 合同编辑 `agreement.save()` 添加 `update_fields`
- 文件: `apps/contract_agreement/views.py` `_post_edit`
- 修改: `agreement.save()` -> `agreement.save(update_fields=save_fields)`
- `save_fields = list(update_data.keys()) + ['updated_at', 'updated_by']`
- 效果: 不再覆盖 Celery 扫描任务的 `status`/`last_remind_at` 并发修改

**R-AP-1 (FIXED)**: 批复 create 的 scan+audit 移入事务
- 文件: `apps/radio_license/approval_views.py` `_handle_create`
- 修改: `scan_single_approval` 和 `_record_approval_audit` 移入 `transaction.atomic()` 块
- 同时处理了 IntegrityError 边界

**R-AP-2 (FIXED)**: 批复 edit 的 scan+audit 移入事务
- 文件: `apps/radio_license/approval_views.py` `_handle_edit`
- 修改: `updated_count == 0` 检查也移入事务内
- `scan_single_approval` 和 `_record_approval_audit` 移入 `transaction.atomic()` 块

**R-AP-3 (FIXED)**: 批复 delete 的 audit 移入事务
- 文件: `apps/radio_license/approval_views.py` `delete`
- 修改: `_record_approval_audit` 移入 `transaction.atomic()` 块（与 `soft_delete` + `approval.delete()` 同事务）

**R-AP-4 (FIXED)**: 批复 ack 的 audit 移入事务
- 文件: `apps/radio_license/approval_views.py` `ApprovalReminderAckView.post`
- 修改: `_record_approval_audit` 移入 `transaction.atomic()` 块（与 `get_or_create` 同事务）

### 变更统计
- `apps/contract_agreement/views.py`: +3/-1 行
- `apps/radio_license/approval_views.py`: +44/-46 行（净减 2 行）

### 验证
- 28/28 测试通过（16 SimpleTestCase + 8 SimpleTestCase + 4 TestCase）
- `py_compile` 语法检查通过
- 无 lint 错误

### R-CA-1/R-CA-2 降级说明
用户反馈：通过 UI 编辑模式测试未发现问题。原因：前端下拉框只传有效 `responsible_user_id`，不传时为 None 被过滤。
风险仅在直接调 API 构造恶意请求时存在，对内部系统属安全加固而非"影响运行稳定"，从 P0 降为 P2。
