# 2026-09-03

## radio_license F-01~F-04 附件越权修复（release_gate）
- 修改仅 2 个文件：`apps/radio_license/views.py`（+103/-22）、`apps/radio_license/tests/release_gate/test_attachments.py`（+234）；未改 evidence 公共服务
- 新增 `_get_license_attachment_for_user()` 统一归属校验（module/object_type/父执照租户可见性/未软删），统一错误文案 `附件不存在或无权限访问`，参照批复侧 `_get_approval_attachment_for_user` 模式
- 根因：
  - F-01/F-02/F-03：执照附件 Download/Delete/PreviewUrl 端点只靠 AttachmentService 的租户过滤，缺 module/object_type/父执照校验 → 同租户合同(contract_agreement/agreement)、协作任务(coop_task/delivery)、批复(radio_license/approval)附件可越权下载/删除/预览
  - F-04：AttachmentDeleteView 软删除后用默认 Manager（TenantModelManager 过滤 is_deleted）再查附件必得 None → EvidenceEvent 死代码；修复为软删除前快照（含 file_name/hash/module/object_type/object_id/tenant_id/delete_reason），删除后用快照写事件
- 关键经验：**本项目 API 约定 HTTP 200 + error 字段，测试不能以 status_code==200 判定成功**；json_response 拒绝时 data=''（key 仍存在，断言用 assertFalse(body.get('data'))）
- 测试：tdyw-test 容器（改码后必须 `docker restart tdyw-test`）跑 test_attachments+test_versions_audit --keepdb：59 例中 57 过；新增 13 个归属校验/证据事件测试全过
- 遗留（既有失败，与附件无关，属版本快照组待修）：
  1. `_save_license_version_snapshot` 硬编码 `changed_fields=''` → test_changed_fields_records_actual_changes 失败
  2. 并发编辑 version_no 竞态（读 max 后 create 无锁）→ test_concurrent_edits_version_consistency 失败 [1,1]
- 其他模块附件常量：contract_agreement/agreement、coop_task/delivery、interference、upgrade、home announcement、signature

## F-01~F-04 独立复核闭环（同日）
- 用户质疑交付报告与 `backend_test_run_final.log` 矛盾 → 查明该日志时间戳 2026-09-03 09:32 是**修复前基线**（255 例/10 失败，含 F-01~F-04 修复前失败记录），非修复后证据
- Docker 实际可用：Windows 侧 `docker ps` 报 npipe 不可达是 Windows 原生 daemon 问题，**走 `wsl bash -c "docker ..."` 即可**；tdyw-test healthy
- 复核日志：`quality/reports/radio_license_release_gate/f01_f04_reverify_20260903_173001.log`（59 例：57 ok / 2 既有失败，均与附件无关）
- ⚠️ verbose 日志中应用日志（AUTH/CSRF）穿插在测试名与 ok 之间，按行窗口匹配判定单测结果不可靠；正确方法：FAIL 块只打印失败用例名，用 FAIL 列表反推通过情况

## radio_license 5 项中危缺陷修复（同日第 3 批）
- 修复范围：ack 校验（责任人/实时状态/租户/幂等，ack_valid_to 只取 DB，get_or_create 对齐批复侧）、列表 status 实时筛选（`_apply_license_status_filter` 按valid_to 范围）、列表 N+1（`_bulk_license_attachment_counts` 批量聚合 + `prefetch_related('frequencies')`）、并发 version_no（`_handle_edit` 事务内 `qs.select_for_update().get(pk)` 行锁）、前端 store 请求守卫（`_fetchSeq` 序列号 + `_active` 卸载标记 + 空页回退 A7）
- 顺手修复 3 个既有失败（均 HEAD 已存在）：`changed_fields` 硬编码 ''（改为编辑事务内 `_detect_license_changed_fields(locked_license, form)` 计算后传入快照+证据事件）、page/page_size 非数字 500（try/except 对齐批复侧）、radioLicense Form.js catch 丢业务错误字符串（`typeof e === 'string' ? e : e.message`，与拦截器 showErrorOnce 2s 去重配合）
- 前端 A7 分页回退测试原 mock 不可满足（所有响应都返回空 records 却断言 records 非空）→ 改为 mockResolvedValueOnce 两次模拟"回退后真实后端返回有数据页"，属测试修正非弱化
- ⚠️ 全量首跑 26 例全报 `deleted_by_id cannot be null`：容器 restart 后紧接跑 test 出现的瞬时状态，重跑即 OK（原因未深究，遇到先重跑再排查）
- 验证：tdyw-test 容器 Django check 通过（仅 document W342 固有 warning）；radio_license 全模块 247 例（含既有 release_gate 并发测试 test_concurrent_edits_version_consistency）全部 OK；Jest radioLicense 17 + stationFrequencyApproval 18 全过；Babel 语法检查 5 文件 OK（NODE_ENV=test + babel-preset-react-app + legacy decorators + class-properties loose）
- 新增测试文件 `apps/radio_license/tests/test_list_ack_version_fixes.py`（26 例：ack 校验 6、popup 一致性 3、status 边界 6、N+1/附件数 4、并发 TransactionTestCase 2、原子性 2、无权限 3）
- ⚠️ write_to_file 在 Windows 写新文件也会产出 CRLF，新建后必须转 LF
- 补充验证：Playwright E2E radio_license.spec.js 4/4 过（tdyw-test localhost:8080）；git diff --check 干净
