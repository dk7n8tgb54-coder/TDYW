# 2026-07-23 工作日志

## 数据库基线、强刷盘与发布阻断检查（Phase 1 持久性）

**背景**：基于《数据库安全与灾备八项问题修复实施方案.md》问题一，实施数据库强刷盘配置 + 发布阻断审计 + 基线采集。

**修改文件**：
1. `docker/config/mysqlnew.cnf`：`innodb_flush_log_at_trx_commit` 2→1，新增 `sync_binlog=1`、`innodb_doublewrite=ON`
2. `scripts/pre_release/audit_config.py`：新增 2b 持久性 + 2c 表引擎 FAIL 检查
3. `database_maintenance/collect_db_baseline.py`（新）：采集版本/镜像digest/参数/引擎/大小/迁移/授权
4. `database_maintenance/DB_DURABILITY_RUNBOOK.md`（新）

## 数据库非 root 化改造（第二阶段）

### 已完成代码（但未生效）
1. settings.py `_read_secret()` 统一 _FILE 读取
2. entrypoint.sh 移除自动 migrate（改 AUTO_MIGRATE 控制）
3. docker-compose.yml tdyw-migrate service + tdyw_app + 非 root 健康检查
4. 备份脚本改 `--defaults-extra-file` + `BACKUP_CLIENT_CNF`
5. audit_config.py 发布阻断检查
6. collect_db_baseline.py 降级处理
7. .env.example + .gitignore 排除 secrets
8. init_db_accounts.sh / revoke_db_accounts.sh / run_init.sh / bootstrap_new_db.sh

### 已执行
- 5 个账号已创建：tdyw_app / tdyw_migrate / tdyw_backup / tdyw_monitor / tdyw_dba
- 授权核验通过：全部符合最小权限设计

### 回退记录（21:30）
- tdyw_app 密码与 .env 不匹配导致应用 500
- root 密码 Dt6299093 也无法从容器内 mysql 客户端连接（原因待查）
- **已回退到 root**：.env MYSQL_USER=root, MYSQL_PASSWORD=Dt6299093
- compose 硬编码 MYSQL_USER=root
- 应用恢复正常，health check 返回 ok

### 教训
- .env 文件 Windows 编辑产生 CRLF，密码带 \r 连接失败
- 方案过度膨胀（5 账号 + _FILE + migrate service + 健康检查 + 备份 + 审计）超出"应用非 root 化"最小范围
- 应先做最小闭环：只创建 tdyw_app + 精确授权 + Django 切换，其余暂缓
- 用户建议正确：砍掉复杂设计，只完成 tdyw_app 最小闭环
