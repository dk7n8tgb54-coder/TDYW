# 2026-07-23 工作日志

## 数据库基线、强刷盘与发布阻断检查（Phase 1 持久性）

**背景**：基于《数据库安全与灾备八项问题修复实施方案.md》问题一，实施数据库强刷盘配置 + 发布阻断审计 + 基线采集。

**生产配置确认**：
- `docker/config/mysqlnew.cnf` 是生产实际配置；entrypoint `mysql-entrypoint.sh` 将其从 `/tmp/mysql-config/custom.cnf` 复制到 `/etc/mysql/conf.d/custom.cnf` 生效。
- compose 挂载：`./config/mysqlnew.cnf:/tmp/mysql-config/custom.cnf:ro`（临时位置，entrypoint 再复制）。

**修改文件**：
1. `docker/config/mysqlnew.cnf`：`innodb_flush_log_at_trx_commit` 2→1，新增 `sync_binlog=1`、`innodb_doublewrite=ON`（binlog 三项已正确保留）。未触碰性能参数。
2. `scripts/pre_release/audit_config.py`：新增"2b 持久性参数(发布阻断)"5 项 FAIL 检查 + "2c 业务表引擎 InnoDB"FAIL 检查。
3. `database_maintenance/collect_db_baseline.py`（新）：采集版本/镜像digest/参数/引擎/大小/迁移/授权，不含密码。通过 stdin 注入 tdyw 运行。
4. `database_maintenance/DB_DURABILITY_RUNBOOK.md`（新）：备份要求/上线步骤/运行态验证/回滚说明。

**验证结果**：
- docker compose config：exit 0
- py_compile 两脚本：OK
- Django check：exit 0（仅 1 个预存 W342 无关警告）
- audit_config.py 运行：2 FAIL（flush=2、sync_binlog=0）= 阻断生效（DB 未重启故运行态仍旧值）；log_bin/binlog_format/doublewrite/表引擎全 PASS
- collect_db_baseline.py 运行：exit 0，72 表全 InnoDB，165 迁移，应用账号=root（问题七待后续阶段）

**关键诚实点**：DB 未重启，强刷盘参数尚未生效，不得宣称已生效。需按 Runbook 重启 tdyw-db 后才生效。

**残余**：Replica 未部署（本阶段不要求）；应用仍用 root 账号（问题七，后续阶段）；gtid/log_slave_updates 留待 Phase 4。
