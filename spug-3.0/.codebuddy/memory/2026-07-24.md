# 2026-07-24 工作日志

## 备份集校验（backups/backup_set_20260724_*）

### backup_set_20260724_023021（旧，有问题）
- 文件清单与实际不符：`SHA256SUMS` 写 `database.sql.gz`，实际文件是 `database.sql`（未压缩 UTF-8 文本，155180 字节）
- manifest.json 中 database artifact 标记 `format=mariadb-dump-gzip`、`prepared=true`、`size=20257`，但实际是 155180 字节纯文本 → sha256 不匹配
- `sha256sum -c` 8 个文件中 database.sql.gz FAILED
- media：manifest 列了 11 个 PNG（286KB），但 media.tar.gz 仅 68 字节（空 tar）、media.delta.json 全空 → 内容空但 metadata 自相矛盾（疑似 incremental vs full 模式判定 bug）
- documents.delta.json（399958 字节）只列了 deleted_directories，符合"清理后空目录"的增量
- base_backup_set_id=020501 不存在

### backup_set_20260724_034320（用户清空 volume 重建后新备份）
**校验全通过** ✅：
- 9 文件齐全；SHA256SUMS 8 个文件全 OK
- database.sql.gz 真 gzip（original size 203939），`gzip -t` 通过，zgrep 72 处 CREATE TABLE
- documents.tar.gz（90MB）/media.tar.gz（823KB）`gzip -t` 通过
- tar 内容与 manifest 完全一致：documents 8 目录+5 文件、media 5 目录+2 文件
- delta.json 与 manifest 一致：documents added 8/5 deleted 0；media added 5/2 deleted 11/11（旧 account_signature 签名图被清空重建正确标记为 deleted）
- 体积合理：PDF/PPTX 已压缩比 documents.tar(90M)≈total(89M)；media.tar(823K)<total(1.5M)
- freeze_seconds=69

**遗留小问题**（非阻塞）：
- manifest.json 的 fileset_chain 仍指向已不存在的备份集：
  - `base_backup_set_id = backup_set_20260724_020501`（不存在）
  - `parent_backup_set_id = backup_set_20260724_023021`（上一个有问题的备份集）
  - `mode = incremental`
- 当前恢复无影响（本次 tar 含完整当前文件，delta 自洽）
- 后续增量备份会持续挂着断链引用
- 建议本次应作为新全量基准：mode 改 full，base/parent 设为自身

### 脚本支持确认
- `create_backup_set_manifest.py --fileset-mode` choices=`("full","incremental")` → full 合法
- `--base-backup-set-id` 必填无默认；脚本会校验 fileset manifest 的 backup_mode/base/parent 三者一致
- 修正需同时改 manifest.json（fileset_chain + filesets.documents + filesets.media）+ documents.manifest.json + media.manifest.json + 重新算 SHA256SUMS

### 上一次 023021 失败根因待查
- `backup_set_create.sh:571` 明确 `| gzip -c > database.sql.gz`，574-576 行有 `[ -s ]`、`gzip -t`、`zgrep CREATE TABLE` 三道校验，正常应 fail-fast
- 023021 却产出了纯文本 `database.sql` —— 推测是用户手动解压了 .gz（或某后处理），但 SHA256SUMS 没同步更新
- 用户自述"有的 volume 重建了有的没有" —— 也可能是源 volume 状态不一致导致 snapshot 异常

## TODO（待用户决定）
- 是否需要我给出"把 034320 改为新全量基准"的具体修正脚本？
- 是否要给 backup_set_create.sh 加 `--reset-base` 开关（方案 2 根治）

## checksheet 模块移除收尾清理

用户已删除 `apps/checksheet/` 整个目录和前端 `pages/checksheet/`，我负责清理其余引用。

### 删除的入口配置
- `spug_api/spug/urls.py`：删 `path('checksheet/', include('apps.checksheet.urls'))`
- `spug_api/spug/settings.py`：从 INSTALLED_APPS 删 `'apps.checksheet'`
- `spug_web/src/routes.js`：删 import + 路由项 + `CheckSquareOutlined` 图标 import（仅此处用）
- `spug_web/src/pages/system/role/codes.js`：删 checksheet 权限码分组
- `spug_web/src/pages/system/audit/Table.js`：删 `checksheet: '检查表'` 类型映射
- `spug_api/apps/logs/audit.py`：删 `/checksheet/` URL→type 映射

### 硬依赖清理（4 个 pdf_export.py）
`runlog`/`duty`/`device`/`department_duty_log` 的 `_register_chinese_font()` 原先 `from apps.checksheet.font_manager import FontManager` 复用字体 + 回退到 `apps/checksheet/fonts/simhei.ttf`。checksheet 删除后这些变成死代码（try/except 兜住不崩，但字体源丢失）。清理：删 try-import 块 + checksheet 字体目录路径，只保留系统字体回退（WindowsFonts / Linux wqy/droid）。

### 测试清理
- 删 `tests/test_checksheet_evidence.py`（纯 checksheet 测试，187 行）
- `tests/test_data_integrity_constraints.py`：删 `from apps.checksheet.models import CheckSheetSubmission`（模块级 import，不删会导致整个测试模块 import 失败）+ 2 个 checksheet 测试方法
- `apps/signature/tests/test_signature_usage.py`：删 `test_checksheet_not_touched`（已变永远 skipTest 的死测试）

### 保留（不动）
- `evidence/models.py` help_text + `evidence/migrations/0001_initial.py`：迁移不可改；DB 里可能仍有 `module='checksheet'` 的历史 EvidenceEvent 记录，help_text 列举该值反而准确
- `evidence/services.py` docstring：同上

### 验证
- `manage.py check`：exitCode 0（仅 1 个 pre-existing DocumentSystemFolder warning，无关）
- `py_compile` 8 个改过的后端文件：全 OK
- 测试 collect：57 tests found（import 成功，无残留导入错误）；实际跑测试因 dev 库用户无 CREATE DATABASE 权限未执行（pre-existing 环境限制）
- 前端 lint：routes.js / codes.js / Table.js 均 0 error

### 变更统计
32 files, +13 / -3215（checksheet 模块自身约 3000 行为用户删除；我清理 13 个引用文件）

### ⚠️ 风险提醒（未解决）
中文字体源 `apps/checksheet/fonts/simhei.ttf` 随模块删除已丢失。4 个 pdf_export 现仅靠系统字体回退。若生产容器未安装中文字体包（如 `fonts-wqy-zenhei`），PDF 导出中文会变方块。需确认容器是否已装中文字体，或在 Dockerfile 加装字体包。

## 字体方案重构（2026-07-24）

用户回退了 checksheet/fonts/simhei.ttf，要求放到 spug_api 合适位置。方案：字体 + 注册逻辑统一迁到 libs。

### 实施
- `apps/checksheet/fonts/simhei.ttf` → `libs/fonts/simhei.ttf`（Move-Item，git 自动识别 rename）
- 新建 `libs/font_manager.py`：迁移原 checksheet/font_manager.py 的 FontManager 类，改 `CONTAINER_FONT_DIR='/data/spug/spug_api/libs/fonts'`，新增模块级 `FONT_NAME='SimHei'` 常量 + `register_chinese_font()` 便捷函数
- 4 个 pdf_export（runlog/duty/device/department_duty_log）：
  - 删除各自 `_FONT_REGISTERED`/`FONT_NAME`/`def _register_chinese_font()` 重复段
  - 改为 `from libs.font_manager import FontManager, FONT_NAME`
  - 调用处 `_register_chinese_font()` → `FontManager.register_chinese_font(debug_logger=logger.debug)`
  - runlog/duty/device 删除 unused `import os`（department_duty_log 保留，line 167 仍用 os.path.exists）
- `apps/checksheet` 目录用户已自行删除

### 关键设计
- FONT_NAME 被各 pdf_export 的 15-19 处 `fontName=FONT_NAME`/`('FONTNAME',...,FONT_NAME)` 引用，从 libs 导入常量后这些引用零改动
- basedpyright 报 `reportImplicitRelativeImport`（from libs.font_manager）是 false positive——项目 91 处都用 `from libs.xxx`，libs 是 spug_api 顶层包

### 验证
- py_compile 5 文件 OK
- 容器内 `from libs.font_manager import FontManager, FONT_NAME` + `register_chinese_font()` 返回 True（bind mount 读到 libs/fonts/simhei.ttf，字体注册成功）
- 残留搜索：4 个 pdf_export 无本地字体定义

### 变更统计
4 个 pdf_export: +8 / -284 行
