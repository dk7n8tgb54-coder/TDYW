# 2026-06-27 工作日志

## 干扰模块剩余项修复（依据《干扰模块修复建议.md》）

承接前次会话已修复的 P0/P1/P2 问题（权限二次校验、服务端筛选分页、导出接口、序号翻页、统计 Substr 聚合、datetime 格式校验），本次修复剩余的 3 项。

### P1：分页参数缺少校验，异常输入触发 500
- 位置：`spug_api/apps/interference/views.py` InterferenceView.get
- 根因：`page = int(request.GET.get('page', 1))` 对 `page=abc` 触发 ValueError；`page=0`/负数造成负切片；`page_size` 无上限
- 修复：新增 `_parse_int(value, name, min_value, max_value)` 通用函数（与 checksheet 模块一致），page>=1，1<=page_size<=200，非法输入返回友好错误而非 500
- 验证：py_compile 通过

### P3：statisticsStore.js 无用 moment import
- 位置：`spug_web/src/pages/interference/statisticsStore.js`
- 修复：删除 `import moment from 'moment';`（dateRange 是外部传入的 moment 对象，调用其 .format() 不需 import moment）
- 验证：read_lints 0 错误

### P3：前端必填校验只显示红星不阻止提交
- 位置：`spug_web/src/pages/interference/Form.js`
- 根因：7 个必填字段用 `required` 属性（仅显示红星），缺 `rules: [{ required: true }]`（实际校验），空值可提交
- 修复：7 个字段（frequency/report_dept/datetime/coordinates/interference_type/phenomenon/is_reported）改用 `rules: [{ required: true, message }]`；frequency 合并 required + pattern 两条规则；移除冗余 `required` 属性（rules 中的 required:true 自动显示红星）
- 验证：read_lints 0 错误

### 变更统计
3 个文件，+41/-11 行。至此《干扰模块修复建议.md》除 P2 datetime 迁移 DateTimeField（中长期，本次跳过）外全部修复完成。

## 干扰模块 P2 问题修复（依据《干扰模块修复建议.md》）

修复《干扰模块修复建议.md》中的 3 个 P2 问题。P0/P1 在之前会话已修复（权限二次校验、服务端筛选分页、导出接口）。

### P2-1：表格序号翻页后仍从 1 开始
- 位置：`spug_web/src/pages/interference/Table.js`
- 根因：render 里读 `store.pagination || {}`，但 store 上根本没有 `pagination` 字段，真实字段是 `pageNum`/`pageSize`，导致 `currentPage` 永远回退成 1
- 修复：改为 `(store.pageNum - 1) * store.pageSize + index + 1`
- 验证：第 2 页第一条序号应为 11，每页 20 条时第 2 页第一条序号应为 21

### P2-2：统计接口按完整时间聚合后再截断日期，日维度数据会重复
- 位置：`spug_api/apps/interference/views.py` InterferenceStatisticsView
- 根因：`filtered_records.values('datetime', 'frequency').annotate(count=Count('id'))` 按 datetime 完整字符串（含时分秒）分组，同一天不同时间会产生多条相同 `date+frequency` 行，Python 层 `datetime[:10]` 截断只改了 key 不合并 count
- 修复：用 `Substr('datetime', 1, 10)` 在数据库层 annotate 出 `date` 字段，再 `values('date', 'frequency').annotate(count=Count('id'))`，数据库直接按日期聚合
- 验证：同一天同频率创建 3 条不同时间记录，统计接口应返回 1 条该日期该频率记录 count=3

### P2-3：日期使用 CharField 保存（短期加格式校验）
- 位置：`spug_api/apps/interference/views.py` InterferenceView.post
- 根因：datetime 是 CharField，列表/统计/首页/值班都依赖 "YYYY-MM-DD HH:MM:SS" 格式，非标准格式会导致筛选失准、排序错乱、今日统计匹配不到
- 短期修复：post 方法在 form 解析后、业务逻辑前用 `datetime.strptime(form.datetime, '%Y-%m-%d %H:%M:%S')` 校验，失败返回错误提示
- 未做：DateTimeField 迁移（中长期，需新增 occurred_at 字段 + 数据迁移 + 前后端切换）

### 验证
- 前端 read_lints 0 错误
- 后端 py_compile COMPILE_OK（tdyw-test 容器）

### 关键经验
- MobX store 字段名要和前端代码严格对应，`store.pagination` 这种不存在的字段会静默回退到默认值，难以发现
- CharField 存日期做聚合时，Python 层截断字符串只改 key 不合并数据，必须在 SQL 层用 Substr 聚合
- 字段类型与业务语义不符（CharField 存日期）是技术债，短期用校验兜底，长期应迁移字段类型

## 测试数据填充脚本修正（tongxinke 租户）

承接 2026-06-26 的种子脚本，修复租户不匹配问题。

### 问题
- 脚本原用 `User.objects.first()`（test_resp_creator, tenant_id=admin），但实际业务账号 cuilinjie 在 tongxinke 租户，导致看不到数据
- 经查：各表均有 admin 和 tongxinke 两个租户的数据

### 修复（`spug_api/seed_data.py`）
- 用户选择改为优先 `User.objects.filter(tenant_id='tongxinke', is_active=True).first()`，回退到任意活跃用户
- 新增 `cleanup_other_tenant_test_data()`：删除非当前租户的 TEST_SEED_ 测试数据（避免脏数据）
- 执行后：清理 admin 租户 50×6=300 条旧数据，tongxinke 租户插入 50×6=300 条新数据

### 关键经验
- 种子脚本取用户时不能简单 `.first()`，要匹配实际业务租户
- 多租户环境下测试数据要明确指定 tenant_id，且要清理其他租户的残留测试数据
- 项目租户：admin（测试用户 test_resp_*）、tongxinke（业务用户 admin/linjie/cuilinjie）

## Django 5.2 LTS 升级 — 阶段0（升级前基线）

依据 `DJANGO_5_2_UPGRADE_PRECHECK.md` 执行阶段0：确认 Django 2.2.28 基线干净，建立可回归基线。

### 执行环境
- 容器 `tdyw-test`（镜像 tdyw:0601，healthy），manage.py 路径 `/data/spug/spug_api/manage.py`
- 数据库 `tdyw-db-test`（mariadb 10.8.2），预览 `tdyw-kkfileview-test`
- Python 3.10
- 命令通过 `docker exec -e PYTHONIOENCODING=utf-8 tdyw-test bash -c 'cd /data/spug/spug_api && ...'` 执行

### 三个预检命令结果（全部 exit 0）
1. `makemigrations --check --dry-run` → No changes detected
2. `migrate --plan` → No planned migration operations
3. `python -Wa manage.py check` → System check identified no issues (0 silenced)

`-Wa` 警告均为旧依赖栈兼容信号：distutils DeprecationWarning（django/utils/version.py）、six ImportWarning、容器 locale 警告，非业务问题。

### 关键经验
- 容器名是 `tdyw-test`（非记忆里的 `tdyw`），镜像 `tdyw:0601`
- 容器内项目路径是 `/data/spug/spug_api`（非 `/spug/spug_api`）
- PowerShell 下 `docker exec bash -c "..."` 内层双引号会导致 bash 解析 EOF 错误，必须改用单引号包裹 bash 脚本：`bash -c '...'`
- 回归场景清单修正：移除"回收站"（该功能已于 2026-06-23 完全移除），资料库回归项改为"上传、分片合并、预览"
- 工作区有未提交的 logs app migration（已 staged），建议进入阶段1前先提交以确保 migration 历史完整

### 基线结论
Django 2.2.28 基线干净，可进入阶段1（2.2 -> 3.2）。阶段0执行记录已写入预检报告"实际执行记录（2026-06-27）"小节。

## Django 3.2 LTS 阶段1升级（2.2.28 -> 3.2.25）

依据 `DJANGO_3_2_STAGE1_UPGRADE_PLAN.md` 完成阶段1升级。完成报告见 `DJANGO_3_2_STAGE1_COMPLETED.md`。

### 修改文件（13 个，+56/-52 行）
- `settings.py`：增加 `DEFAULT_AUTO_FIELD = 'django.db.models.AutoField'`
- `requirements.txt`：Django 2.2.28->3.2.25, asgiref 3.2.10->3.11.1, mysqlclient 1.4.6->2.x, django-redis 4.10.0->5.4.0
- 6 个 urls.py：`django.conf.urls.url` -> `django.urls.re_path`（等价替换）
- 4 个 apps.py：`BigAutoField` -> `AutoField`（device/interference/document/radio_license）
- `dev/docker-compose.yml`：镜像 `tdyw:0601` -> `tdyw:django32-stage1`

### 关键发现与修复
1. **django-redis 4.x 不兼容 Django 3.2**：`from django.utils import six` 在 Django 3.0 已移除，必须升级到 django-redis 5.x
2. **4 个 apps.py 的 BigAutoField 设置错误**：模型在 Django 2.2（默认 AutoField）下创建，apps.py 却设了 BigAutoField，导致 Django 3.2 生成 `Alter field id` 迁移噪音。改为 AutoField 与数据库一致后，makemigrations 恢复干净
3. **Django 3.2 内置迁移 auth.0012**：first_name max_length 30->150，正常升级迁移，已应用

### 验证结果
- check/makemigrations/migrate 三命令全 exit 0
- 12 个 supervisor 进程全部 RUNNING
- Celery 5 nodes online（merge/cleanup/default/batch/radio-license）
- 18+ 核心 API 回归通过（登录/用户/设备/干扰/执照/审计/导出/资料库）
- WebSocket 握手 101 Switching Protocols
- Docker 镜像 `tdyw:django32-stage1` 构建成功，容器 healthy

### 遗留 Warning（阶段2待处理）
- 5 个 `RemovedInDjango41Warning`：apps.schedule/upgrade/checksheet/logs + channels 的 `default_app_config` 已废弃，Django 4.1 移除

### 阶段2（3.2 -> 4.2）待处理项
1. 删除 5 个 app 的 `default_app_config`
2. 删除 `USE_L10N`
3. 升级 Channels 2.x -> 4.x（+ channels_redis + daphne）
4. pytz -> zoneinfo（libs/helper.py）

### 关键经验
- 容器使用系统 Python `/usr/bin/python`（非 venv），pip install 直接装到系统 site-packages
- 容器内 mysqlclient 编译需要 `pkg-config` + `default-libmysqlclient-dev`，镜像已包含
- `docker run --rm` 临时容器不连数据库，验证镜像内包版本用 `--entrypoint python -c "..."`
- PowerShell 下 docker stderr 会被当作 CLIXML 错误流，实际命令可能成功（看 exitCode 和 stdout）

## Django 4.2 阶段2验收（3.2.25 -> 4.2.30）

### 验收环境
- 容器 `tdyw-test`（镜像 `tdyw:django42-stage2`，healthy）
- 数据库 `tdyw-db-test`（mariadb 10.8.2）

### 实际依赖版本
- Django 4.2.30, asgiref 3.11.1, channels 4.3.2（验收中从 3.0.5 升级）, channels_redis 4.3.0, daphne 4.2.2
- celery 5.2.7, django_celery_beat 2.9.0, django_celery_results 2.4.0, django_redis 5.4.0, mysqlclient 2.1.1

### 检查命令结果（全 exit 0）
1. `python manage.py check` → System check identified no issues
2. `python -Wa manage.py check` → System check identified no issues（**无 RemovedInDjango50Warning**）
3. `python manage.py makemigrations --check --dry-run` → No changes detected
4. `python manage.py migrate --plan` → No planned migration operations

### Supervisor（12/12 RUNNING）
nginx/redis/spug-api/spug-api-upload/spug-ws/spug-worker/spug-celery-batch/merge/cleanup/default/radio-license/beat 全部 RUNNING

### Celery（5 nodes online）
cleanup/radio-license/default/batch/merge 全部 pong

### 核心 API 回归（全部 HTTP 200）
登录(self/role/tenant/user)/设备/干扰(列表+统计+导出10KB)/运行日志/日检查单/系统升级/审计日志(16617条+导出1.4MB)/无线电执照(列表+角标+提醒)/首页统计/系统设置(about确认django_version=4.2.30)/资料库(folder/disk/health/transfers)/退出

### 资料库上传/下载/删除（全 HTTP 200）
普通上传→列表确认→下载内容正确→删除→列表空，链路完整

### WebSocket 握手
直连9002 + 经nginx80 均返回 `HTTP/1.1 101 Switching Protocols`

### 验收中修复的问题：Channels 2.x -> 4.x WebSocket 兼容（3 个文件）

**根因**：Channels 2.x 在 `__init__` 时设置 `self.scope`，3.x/4.x 改为在 `__call__` 时设置；且 Channels 4.x 的 `URLRouter` 不再自动对 consumer 类做 `guarantee_single_callable` 转换。

**修复 1 — `consumer/consumers.py`**：
- `PubSubConsumer` 和 `ComConsumer` 的 `__init__` 中访问 `self.scope` 的逻辑移到 `init()` 方法（`BaseConsumer.connect()` 验证用户后调用的钩子）
- `disconnect()` 加 `hasattr` 保护（连接失败时 rds/p 可能未初始化）

**修复 2 — `consumer/routing.py`**：
- consumer 类注册改为 `PubSubConsumer.as_asgi()` / `ComConsumer.as_asgi()`
- Channels 4.x 要求 consumer 类通过 `as_asgi()` 转为 single-callable ASGI 应用

**修复 3 — `spug/asgi.py`**：
- Channels 4.x 移除了 `get_default_application()`，改为 `from spug.routing import application`

**修复 4 — `requirements.txt`**：
- channels/channels_redis/daphne 从 `>=3,<4` 改为 `>=4,<5`（阶段2计划只升到 3.x，但 3.x 与 asgiref 3.11 有兼容 bug，必须升到 4.x）

**关键排查经验**：
- `is_double_callable()` 对"类"无条件返回 True（`inspect.isclass(application)`），导致 Channels 4.x URLRouter `await application(scope,...)` 把 consumer 类当 double-callable 误处理
- `as_asgi()` 返回 coroutine function（single-callable），绕过 `is_double_callable` 误判
- 错误演进：`self.scope AttributeError`（__init__ 问题）→ `AsyncConsumer.__call__ missing 'send'`（3.x 兼容 bug）→ `can't be used in await`（未用 as_asgi）→ `101 Switching Protocols`（修复成功）

### 遗留问题（非 Django 4.2 阻断项）
- `celery_cleanup_error.log` 有 "Received unregistered task"：settings.py 的 CELERY_TASK_ROUTES/CELERY_BEAT_SCHEDULE 任务名（`apps.document.tasks.cleanup.retry_clean_pending_files`、`apps.document.tasks.cleanup_old_chunks`）与重构后实际模块路径（`apps.document.tasks.cleanup.pending_files.retry_clean_pending_files`、`apps.document.tasks.cleanup.chunks.cleanup_old_chunks`）不一致，属资料库任务模块重构遗留 bug，非升级引入
- ws_err.log 历史 `ValueError: No route found for path ''`：daphne 对空路径探测请求的响应，非升级问题

### 镜像重建提醒
当前容器内 Channels 4.x 是 pip 临时安装的，镜像 `tdyw:django42-stage2` 仍是 Channels 3.x。需用更新后的 requirements.txt 重新构建镜像才能固化。

### 阶段2成功标准达成情况
✅ Django 4.2.30 / check 无错误 / -Wa 无 Django4 阻断 warning / makemigrations No changes / migrate plan 无非预期迁移 / 镜像可启动 / 12 进程 RUNNING / 登录和主要页面可访问 / Celery worker+beat 正常 / WebSocket 正常 / 文件上传/下载/删除正常 / 导出正常

## 资料库模块前后端 4 项修复

修复资料库搜索租户隔离、合并分页 has_more、前端 API 常量路径、clearCompletedTransfers 语义错误。

### 1. 私有空间搜索漏租户过滤（安全）
- 位置：`spug_api/apps/document/views/search.py` FolderSearchView.get
- 根因：folders_query 在非公共空间调了 apply_tenant_filter，files_query 没调，folder_id=None 的根目录文件可能跨租户泄露
- 修复：files_query 构造后、count 前，`if not form.is_public: files_query = apply_tenant_filter(files_query, request.user, strict_mode=True)`
- 注意：search.py 自身的 has_more 用 `max(total_folders, total_files)` 是正确的（folders/files 独立切片，max 表示"任一列表还有更多"），与 folder/views.py 的合并分页语义不同，无需改

### 2. 文件夹/文件合并分页 has_more 不准
- 位置：`spug_api/apps/document/views/folder/views.py` _get_root_contents / _get_folder_contents
- 根因：合并分页（文件夹在前文件在后）用 `max(total_folders, total_files)` 判断 has_more，应按两者总数
- 修复：两处引入 `total_items = total_folders + total_files`，has_more 改为 `(offset + page_size) < total_items`

### 3. 前端 API 常量路径与后端不对齐
- 位置：`spug_web/src/pages/document/stores/constants/api.js`
- 先搜索确认 FILE_DOWNLOAD/FILE_PREVIEW/SEARCH/DISK_INFO/FOLDER_TREE 在 API_ENDPOINTS.XXX 形式下均无调用方
- 修正：FILE_DOWNLOAD `/file/download/`→`/download/`；FILE_PREVIEW `/file/preview/`→`/preview/`；SEARCH `/search/`→`/folder/search/`；DISK_INFO `/disk/`→`/disk_usage/`
- FOLDER_TREE：后端无此路由且无调用方，保留但加 `@deprecated` 注释避免误导

### 4. clearCompletedTransfers 语义错误
- 位置：`spug_web/src/pages/document/stores/upload/core/transfer.js`
- 根因：调用 TRANSFERS_BATCH_CANCEL 且不传 transfer_ids，会取消所有传输任务，与"清理已完成"语义不符
- 经搜索确认全项目无调用方
- 修复：改为安全 no-op + console.warn + 注释说明后端暂无专用接口，如需清理应用 batchDeleteTransfers(已完成ID列表)

### 验证
- read_lints 0 错误（4 个改动文件）
- 后端 `python -m compileall apps/document` exit 0（tdyw-test 容器，search.py + folder/views.py 均编译通过）
- 前端 `npm run build` exit 0（构建成功，仅有 runlog/Table.js、useColumns.js 等无关文件的预存 lint 警告）
- 无现成资料库搜索/分页测试可跑（前端测试仅上传相关，后端 apps/document 无 test 文件）

### 关键经验
- 合并分页的 has_more 必须按总数判断，独立切片分页用 max 才对——两种语义不能混用
- 改 API 常量前必须先 grep 调用方，避免误删/误改在用导出；本次确认 5 个常量均无调用才改路径
- 未被调用的方法误用危险接口（batch cancel 不传 ids）是隐性 bug，宁可 no-op 也别留危险实现

## 资料库传输列表状态机修复（7 项问题）

依据《资料库传输状态机修复建议.md》修复前端状态集合混用、cancelItem 绕过状态机、后端同步重复、_directMerge 绕过状态机、批量恢复漏任务、后端统计不准、清空接口语义 7 项问题。

### 修改文件（17 个）

**前端核心（10 个）**：
- `upload-core-constants.js`：新增 `SLOT_OCCUPYING_STATUSES`/`PAUSEABLE_STATUSES`/`DISPLAY_UPLOADING_STATUSES` 三个语义化常量
- `UploadStateMachine.js`：新增 `RETRY_MERGE` 事件（error/waiting → merging）；移除 entry 钩子中的 `updateTransferStatus` 调用（消除重复同步）
- `StateMachineManager.js`：`batchPause` 改用 `PAUSEABLE_STATUSES`；`batchResume` 改用 `SLOT_OCCUPYING_STATUSES` 计数
- `ItemOperationController.js`：`cancelItem` 改为通过 `transition('CANCEL')` 进入 cancelled 终态（与批量取消一致）；`_directMerge` 改为通过 `transition('RETRY_MERGE')` 进入 merging
- `QueueOperationController.js`：`removeAll` 改用 `DISPLAY_UPLOADING_STATUSES`（修复遗漏 waiting/paused 的 bug）
- `DebounceController.js`：`resumeAll` 新增 `startWaiting()` 调用覆盖懒创建状态机的 waiting 任务；改用 `SLOT_OCCUPYING_STATUSES`
- `StateChangeHandler.js`：`handleMergingState` 传入 event 参数，`RETRY_MERGE` 路径跳过 `mergeChunks`（_directMerge 已处理）
- `index.js`：`uploadingItems`/`activeCount` 改用 `DISPLAY_UPLOADING_STATUSES`（修复 activeCount 遗漏 waiting 的 bug）
- `sync/StatusSynchronizer.js`：移除 `@action` 装饰器（jest 环境不支持 decorator 语法，导致测试无法加载）
- `StoreEventAdapter.js`：`handleTransferStatusUpdate` 添加降级注释

**前端 UI（4 个）**：
- `TransferItem.js`：`canPause` 改用 `PAUSEABLE_STATUSES`；`canRemove` 改用 `TERMINAL_STATUSES`
- `TransferList.js`：`isUploadingStatus` 改用 `DISPLAY_UPLOADING_STATUSES`
- `VirtualTransferList.js`：`pauseableCount` 改用 `PAUSEABLE_STATUSES`（修复误将 merging 计为可暂停的 bug）
- `UploadPanel.js`：`componentWillUnmount` 改用 `PAUSEABLE_STATUSES`（修复尝试暂停 merging 的 bug）；MiniBar 改用 `DISPLAY_UPLOADING_STATUSES`/`TERMINAL_STATUSES`

**协调器（2 个）**：
- `UploadCoordinator.js`：`startWaiting` 改用 `SLOT_OCCUPYING_STATUSES`
- `RecoveryCoordinator.js`：`countByStates` 改用 `SLOT_OCCUPYING_STATUSES`

**后端（1 个）**：
- `batch.py`：`TransferBatchPauseView`/`TransferBatchResumeView` 返回 `updated_ids`/`already_ids`/`skipped_ids`，区分"新更新"和"原本已在此状态"

**前端 Store（1 个）**：
- `transfer.js`：`batchPauseTransfers`/`batchResumeTransfers` 使用 `already_ids` 展示更准确的统计文案

**测试（2 个）**：
- `StateMachineManager.test.js`：修复 4 个 pre-existing 失败测试（mock item 添加 `totalChunks: 5`，终态隔离）；新增 4 个测试（RETRY_MERGE、单个/批量取消一致性、批量取消后状态）
- `StatusSynchronizer.test.js`：新增 7 个测试（error 终态同步、cancelled 阻止 merging 回退、error 阻止 uploading 回退、waiting 不同步、去重、paused/merging 同步）

### 验证结果
- 单元测试：4 suite / 58 test 全部通过
- 前端构建：`npm run build` exit 0（仅有无关文件的预存 lint 警告）
- read_lints：所有改动文件 0 错误

### 关键设计决策
1. **cancelItem 改为"取消并保留记录"**：与批量取消语义一致，transition('CANCEL') 进入 cancelled 终态，用户可通过"删除"按钮永久移除
2. **RETRY_MERGE 事件**：error/waiting → merging 的快捷路径，避免重传分片；StateChangeHandler 检测到 RETRY_MERGE 事件时跳过 mergeChunks（_directMerge 已调 DIRECT_MERGE 接口）
3. **后端同步唯一链路**：StateChangeHandler → StatusSynchronizer.syncStateToBackend()，移除 entry 钩子中的 actions.updateTransferStatus() 调用
4. **SLOT_OCCUPYING_STATUSES 不含 merging**：merging 是后端 Celery 任务，前端只轮询，不占前端网络/CPU 槽位
5. **PAUSEABLE_STATUSES 不含 merging/paused**：merging 不可暂停（状态机无 PAUSE 转换），paused 不可重复暂停

### 待确认的产品语义
- cancelItem 改为保留记录后，用户需要点"删除"才能永久移除——如果产品希望取消后直接消失，需改回 removeItem 并区分文案

## 资料库传输列表状态机修复（第二轮强化）

承接第一轮修复，按用户更严格的产品语义要求做 4 项强化。

### 修改文件（5 个）
1. `controls/ItemOperationController.js` — `_directMerge` 重构：状态机重建失败或不可 RETRY_MERGE 时不再悄悄直接写 `item.status='merging'`，而是返回 false 让调用方走 RESUME 降级；先验证状态机可用再调 DIRECT_MERGE 接口（避免后端已开始合并但前端状态机仍 error/waiting）
2. `controls/DebounceController.js` — `_doResumeAll` 重构：改为按队列 item 扫描 paused 任务（不再只遍历 machines），paused 无状态机时先 ensureStateMachine 再 transition('RESUME')；waiting 交给 startWaiting 调度
3. `StoreEventAdapter.js` — `handleTransferStatusUpdate` 改为委托 statusSynchronizer.shouldSync/markSynced 做去重，与权威链路共享 _lastSyncStatusMap
4. `sync/StatusSynchronizer.js` — 抽出 `shouldSync(transferId, backendStatus)` 和 `markSynced(transferId, backendStatus)` 公共方法，供 StoreEventAdapter 共用
5. `index.js` — `_initEventAdapter` 传入 `statusSynchronizer` 引用

### 测试（2 个文件，新增 8 个测试）
- `StateMachineManager.test.js`：新增 4 个测试（批量恢复跳过终态/merging、completed 不可回滚、merging 不可暂停、cancelled 不可 RETRY_MERGE）
- `StatusSynchronizer.test.js`：新增 4 个测试（shouldSync 首次/重复/状态变化、StoreEventAdapter 共用去重模拟）

### 验证结果
- 单元测试：4 suite / **66 test 全部通过**（上轮 58，本轮 +8）
- 前端构建：`npm run build` exit 0
- read_lints：所有改动文件 0 错误

### 关键设计决策
1. **_directMerge 不再降级直接写 merging**：状态机不可用时返回 false → 调用方 resumeItem 继续走 RESUME → 状态机 ERROR → onErrorEntry → 用户看到"合并失败，请重试"。避免 UI 显示 merging 但状态机仍在 error 的分裂
2. **批量恢复按队列 item 扫描**：pausedItems 从 `queue.filter(status==='paused')` 获取，不依赖 machines Map；paused 无状态机时 ensureStateMachine 重建，重建后为 waiting 则交给 startWaiting
3. **StoreEventAdapter 去重共用**：shouldSync/markSynced 是 StatusSynchronizer 的公共方法，StoreEventAdapter 调用前先检查，避免同一状态通过两条链路重复请求后端
4. **clearCompletedTransfers 已是 no-op**：经搜索确认全项目无调用方，UploadPanel 直接用 batchDeleteTransfers 清空，语义正确
