# 2026-06-27 工作日志

## 证据闭环优化方案 - 第一阶段：全局操作审计增强

依据仓库根目录《证据闭环优化方案.md》分阶段实施，本次完成第一阶段（仅改 logs 模块，不碰业务模块）。

### 改动文件
- 新增 `apps/logs/hash_chain.py`：哈希链工具（compute_request_hash/response_hash/log_hash、verify_log_hash、verify_hash_chain）
- 修改 `apps/logs/models.py`：AuditLog 增加 6 字段 request_hash/response_hash/prev_hash/log_hash/request_id/user_agent + 3 索引
- 新增 `apps/logs/migrations/0003_audit_hash_fields.py`：6 AddField + 3 AddIndex，全部可空/默认，兼容旧数据
- 修改 `apps/logs/audit.py`：save_audit_log 在事务内查同租户上一条 log_hash 作 prev_hash，显式生成 created_at 保证 log_hash 输入与落库一致；装饰器也采集 user_agent/request_id/response_hash；新增 _extract_user_agent
- 修改 `apps/logs/middleware.py`：process_request 生成 uuid4 request_id 存 request._audit_request_id；process_response 采集 user_agent/response_hash 传入 save_audit_log
- 修改 `apps/logs/views.py`：AuditLogExportView 导出后调用 _record_export_audit 记录 action=export/target_type=audit（记录筛选条件+导出数量+导出人），失败不阻断导出
- 新增 `tests/test_audit_hash_chain.py`：31 个测试

### 关键设计决策
- log_hash = SHA256(sort_keys JSON(全部关键字段 + prev_hash))，按租户成链
- request_hash 基于"存库 detail 字符串"计算（非原始 body），可证明 detail 未被篡改
- prev_hash 取同租户上一条 log_hash，乐观查询（内网低并发，偶发分叉靠每日摘要校验发现）
- 旧数据 log_hash='' → verify_log_hash 返回 False（不阻断业务）
- 新增字段全部 default='' 或 null，向后兼容现有调用

### 验证
- py_compile 通过
- makemigrations logs --dry-run: No changes detected（migration 与 models 一致）
- migrate logs: 0003 应用成功
- python manage.py test tests.test_audit_hash_chain --noinput: 31 tests OK
- 容器：tdyw-test（tdyw:django42-stage2），项目路径 /data/spug/spug_api，测试库 test_spug 需 --noinput 自动重建

### 注意
- test_spug 库已存在时需加 --noinput（否则非交互 EOF）
- makemigrations 必须指定模块名 logs（记忆教训）
- 后续阶段（附件哈希/状态流转/证据事件/证据包）未开始，本阶段仅审计底座

## 证据闭环优化方案 - 第二阶段：统一证据底座

依据《证据闭环优化方案.md》第 3.2 节，新建 `apps/evidence` app 承载跨业务模块共用的证据事件表与附件证据表。本阶段只建表+服务+测试，不接入任何业务模块视图/信号，不破坏现有业务流程。

### 改动文件
- 新增 `apps/evidence/`（__init__/apps.py/models.py/hash.py/services.py/migrations/0001_initial.py）
- 修改 `spug/settings.py`：INSTALLED_APPS 注册 `apps.evidence`（1 行）
- 新增 `tests/test_evidence_stage2.py`：31 个测试

### 模型设计
- `EvidenceEvent`（tdyw_evidence_events）：证据事件表
  - 业务对象定位：module + object_type + object_id
  - event_type：submit/approve/reject/close/correct/delete/export/void/other
  - 身份快照：actor_user_id + actor_username + actor_name + actor_department + actor_ip + actor_device
  - 快照：object_snapshot/before_snapshot/after_snapshot/attachment_hashes（JSON 字符串）
  - 哈希链：prev_hash + event_hash，按业务对象(tenant_id+module+object_type+object_id)成链
  - 关联：audit_log_id（对应全局审计日志 ID）
  - 预留：external_ts_provider/external_ts_token（第三方时间戳，内网留空）
- `EvidenceAttachment`（tdyw_evidence_attachments）：附件证据表
  - file_hash_sha256 + file_hash_md5
  - 软删除：is_deleted/deleted_by_id/deleted_by_name/deleted_at/delete_reason
  - 上传人快照：uploaded_by_id + uploaded_by_name

### 服务设计（services.py）
- `record_evidence_event()`：业务模块写入证据事件统一入口
  - 事务内查同一业务对象链上一条 event_hash 作 prev_hash
  - 显式生成 created_at 保证 event_hash 输入与落库一致
  - 快照字段接受 dict/JSON str/None，dict 自动 json.dumps(sort_keys=True)
  - 异常不抛出（不阻断业务主流程），返回 None
- `compute_attachment_hash()`：流式计算附件 SHA256，重置文件指针
- `VALID_EVENT_TYPES`：合法事件类型集合，非法类型返回 None

### hash.py 设计
- event_hash = SHA256(sort_keys JSON(tenant_id+module+object_type+object_id+event_type+actor_user_id+actor_username+actor_name+object_snapshot+attachment_hashes+prev_hash+created_at))
- verify_event_hash：单条防篡改校验，旧数据空 hash 返回 False
- verify_event_chain：同一业务对象链连续性校验

### 验证
- py_compile 通过；lint 无问题
- makemigrations evidence --dry-run: No changes detected（migration 与 models 一致，所有 Index 显式 name）
- migrate evidence: 0001_initial 应用成功
- python manage.py test tests.test_evidence_stage2 --noinput: 31 tests OK
- 测试覆盖：hash 确定性/sort_keys/篡改检测/链连续性/链断裂/业务对象隔离/module隔离/身份快照/快照序列化/非法类型拒绝/合法类型全覆盖/异常不抛出/附件哈希流式计算/软删除/租户筛选/to_dict

### 教训
- models.py 中 Index 必须显式指定 name，否则 Django makemigrations 会生成与手写 migration 不同的自动名，导致"重命名索引"的假 migration。本次给 3 个无 name 的索引补 name（ev_obj_actor_idx/ev_obj_type_idx/ev_att_del_idx）后 dry-run 变 No changes
- 后续阶段（第三阶段提交/复核/锁定、第四阶段证据事件接入业务、第五阶段归档）未开始

## 干扰模块剩余项修复（依据《干扰模块修复建议.md》）

承接前次会话已修复的 P0/P1/P2 问题（权限二次校验、服务端筛选分页、导出接口、序号翻页、统计 Substr 聚合、datetime 格式校验），本次修复剩余的 3 项。

### P1：分页参数缺少校验，异常输入触发 500
- 位置：`spug_api/apps/interference/views.py` InterferenceView.get
- 根因：`page = int(request.GET.get('page', 1))` 对 `page=abc` 触发 ValueError；`page=0`/负数造成负切片；`page_size` 无上限
- 修复：新增 `_parse_int(value, name, min_value, max_value)` 通用函数（与 checksheet 模块一致），page>=1，1<=page_size<=200，非法输入返回友好错误而非 500
- 验证：py_compile 通过

### P3：statisticsStore.js 无用 moment import
- 位置：`spug_web/src/pages/interference/statisticsStore.js`
- 修复：删除 `import moment from 'moment';`（dateRange 是外部传入的 moment 对象，调用其 .format() 不需 import moment）
- 验证：read_lints 0 错误

### P3：前端必填校验只显示红星不阻止提交
- 位置：`spug_web/src/pages/interference/Form.js`
- 根因：7 个必填字段用 `required` 属性（仅显示红星），缺 `rules: [{ required: true }]`（实际校验），空值可提交
- 修复：7 个字段（frequency/report_dept/datetime/coordinates/interference_type/phenomenon/is_reported）改用 `rules: [{ required: true, message }]`；frequency 合并 required + pattern 两条规则；移除冗余 `required` 属性（rules 中的 required:true 自动显示红星）
- 验证：read_lints 0 错误

### 变更统计
3 个文件，+41/-11 行。至此《干扰模块修复建议.md》除 P2 datetime 迁移 DateTimeField（中长期，本次跳过）外全部修复完成。

## 干扰模块 P2 问题修复（依据《干扰模块修复建议.md》）

修复《干扰模块修复建议.md》中的 3 个 P2 问题。P0/P1 在之前会话已修复（权限二次校验、服务端筛选分页、导出接口）。

### P2-1：表格序号翻页后仍从 1 开始
- 位置：`spug_web/src/pages/interference/Table.js`
- 根因：render 里读 `store.pagination || {}`，但 store 上根本没有 `pagination` 字段，真实字段是 `pageNum`/`pageSize`，导致 `currentPage` 永远回退成 1
- 修复：改为 `(store.pageNum - 1) * store.pageSize + index + 1`
- 验证：第 2 页第一条序号应为 11，每页 20 条时第 2 页第一条序号应为 21

### P2-2：统计接口按完整时间聚合后再截断日期，日维度数据会重复
- 位置：`spug_api/apps/interference/views.py` InterferenceStatisticsView
- 根因：`filtered_records.values('datetime', 'frequency').annotate(count=Count('id'))` 按 datetime 完整字符串（含时分秒）分组，同一天不同时间会产生多条相同 `date+frequency` 行，Python 层 `datetime[:10]` 截断只改了 key 不合并 count
- 修复：用 `Substr('datetime', 1, 10)` 在数据库层 annotate 出 `date` 字段，再 `values('date', 'frequency').annotate(count=Count('id'))`，数据库直接按日期聚合
- 验证：同一天同频率创建 3 条不同时间记录，统计接口应返回 1 条该日期该频率记录 count=3

### P2-3：日期使用 CharField 保存（短期加格式校验）
- 位置：`spug_api/apps/interference/views.py` InterferenceView.post
- 根因：datetime 是 CharField，列表/统计/首页/值班都依赖 "YYYY-MM-DD HH:MM:SS" 格式，非标准格式会导致筛选失准、排序错乱、今日统计匹配不到
- 短期修复：post 方法在 form 解析后、业务逻辑前用 `datetime.strptime(form.datetime, '%Y-%m-%d %H:%M:%S')` 校验，失败返回错误提示
- 未做：DateTimeField 迁移（中长期，需新增 occurred_at 字段 + 数据迁移 + 前后端切换）

### 验证
- 前端 read_lints 0 错误
- 后端 py_compile COMPILE_OK（tdyw-test 容器）

### 关键经验
- MobX store 字段名要和前端代码严格对应，`store.pagination` 这种不存在的字段会静默回退到默认值，难以发现
- CharField 存日期做聚合时，Python 层截断字符串只改 key 不合并数据，必须在 SQL 层用 Substr 聚合
- 字段类型与业务语义不符（CharField 存日期）是技术债，短期用校验兜底，长期应迁移字段类型

## 测试数据填充脚本修正（tongxinke 租户）

承接 2026-06-26 的种子脚本，修复租户不匹配问题。

### 问题
- 脚本原用 `User.objects.first()`（test_resp_creator, tenant_id=admin），但实际业务账号 cuilinjie 在 tongxinke 租户，导致看不到数据
- 经查：各表均有 admin 和 tongxinke 两个租户的数据

### 修复（`spug_api/seed_data.py`）
- 用户选择改为优先 `User.objects.filter(tenant_id='tongxinke', is_active=True).first()`，回退到任意活跃用户
- 新增 `cleanup_other_tenant_test_data()`：删除非当前租户的 TEST_SEED_ 测试数据（避免脏数据）
- 执行后：清理 admin 租户 50×6=300 条旧数据，tongxinke 租户插入 50×6=300 条新数据

### 关键经验
- 种子脚本取用户时不能简单 `.first()`，要匹配实际业务租户
- 多租户环境下测试数据要明确指定 tenant_id，且要清理其他租户的残留测试数据
- 项目租户：admin（测试用户 test_resp_*）、tongxinke（业务用户 admin/linjie/cuilinjie）

## Django 5.2 LTS 升级 — 阶段0（升级前基线）

依据 `DJANGO_5_2_UPGRADE_PRECHECK.md` 执行阶段0：确认 Django 2.2.28 基线干净，建立可回归基线。

### 执行环境
- 容器 `tdyw-test`（镜像 tdyw:0601，healthy），manage.py 路径 `/data/spug/spug_api/manage.py`
- 数据库 `tdyw-db-test`（mariadb 10.8.2），预览 `tdyw-kkfileview-test`
- Python 3.10
- 命令通过 `docker exec -e PYTHONIOENCODING=utf-8 tdyw-test bash -c 'cd /data/spug/spug_api && ...'` 执行

### 三个预检命令结果（全部 exit 0）
1. `makemigrations --check --dry-run` → No changes detected
2. `migrate --plan` → No planned migration operations
3. `python -Wa manage.py check` → System check identified no issues (0 silenced)

`-Wa` 警告均为旧依赖栈兼容信号：distutils DeprecationWarning（django/utils/version.py）、six ImportWarning、容器 locale 警告，非业务问题。

### 关键经验
- 容器名是 `tdyw-test`（非记忆里的 `tdyw`），镜像 `tdyw:0601`
- 容器内项目路径是 `/data/spug/spug_api`（非 `/spug/spug_api`）
- PowerShell 下 `docker exec bash -c "..."` 内层双引号会导致 bash 解析 EOF 错误，必须改用单引号包裹 bash 脚本：`bash -c '...'`
- 回归场景清单修正：移除"回收站"（该功能已于 2026-06-23 完全移除），资料库回归项改为"上传、分片合并、预览"
- 工作区有未提交的 logs app migration（已 staged），建议进入阶段1前先提交以确保 migration 历史完整

### 基线结论
Django 2.2.28 基线干净，可进入阶段1（2.2 -> 3.2）。阶段0执行记录已写入预检报告"实际执行记录（2026-06-27）"小节。

## Django 3.2 LTS 阶段1升级（2.2.28 -> 3.2.25）

依据 `DJANGO_3_2_STAGE1_UPGRADE_PLAN.md` 完成阶段1升级。完成报告见 `DJANGO_3_2_STAGE1_COMPLETED.md`。

### 修改文件（13 个，+56/-52 行）
- `settings.py`：增加 `DEFAULT_AUTO_FIELD = 'django.db.models.AutoField'`
- `requirements.txt`：Django 2.2.28->3.2.25, asgiref 3.2.10->3.11.1, mysqlclient 1.4.6->2.x, django-redis 4.10.0->5.4.0
- 6 个 urls.py：`django.conf.urls.url` -> `django.urls.re_path`（等价替换）
- 4 个 apps.py：`BigAutoField` -> `AutoField`（device/interference/document/radio_license）
- `dev/docker-compose.yml`：镜像 `tdyw:0601` -> `tdyw:django32-stage1`

### 关键发现与修复
1. **django-redis 4.x 不兼容 Django 3.2**：`from django.utils import six` 在 Django 3.0 已移除，必须升级到 django-redis 5.x
2. **4 个 apps.py 的 BigAutoField 设置错误**：模型在 Django 2.2（默认 AutoField）下创建，apps.py 却设了 BigAutoField，导致 Django 3.2 生成 `Alter field id` 迁移噪音。改为 AutoField 与数据库一致后，makemigrations 恢复干净
3. **Django 3.2 内置迁移 auth.0012**：first_name max_length 30->150，正常升级迁移，已应用

### 验证结果
- check/makemigrations/migrate 三命令全 exit 0
- 12 个 supervisor 进程全部 RUNNING
- Celery 5 nodes online（merge/cleanup/default/batch/radio-license）
- 18+ 核心 API 回归通过（登录/用户/设备/干扰/执照/审计/导出/资料库）
- WebSocket 握手 101 Switching Protocols
- Docker 镜像 `tdyw:django32-stage1` 构建成功，容器 healthy

### 遗留 Warning（阶段2待处理）
- 5 个 `RemovedInDjango41Warning`：apps.schedule/upgrade/checksheet/logs + channels 的 `default_app_config` 已废弃，Django 4.1 移除

### 阶段2（3.2 -> 4.2）待处理项
1. 删除 5 个 app 的 `default_app_config`
2. 删除 `USE_L10N`
3. 升级 Channels 2.x -> 4.x（+ channels_redis + daphne）
4. pytz -> zoneinfo（libs/helper.py）

### 关键经验
- 容器使用系统 Python `/usr/bin/python`（非 venv），pip install 直接装到系统 site-packages
- 容器内 mysqlclient 编译需要 `pkg-config` + `default-libmysqlclient-dev`，镜像已包含
- `docker run --rm` 临时容器不连数据库，验证镜像内包版本用 `--entrypoint python -c "..."`
- PowerShell 下 docker stderr 会被当作 CLIXML 错误流，实际命令可能成功（看 exitCode 和 stdout）

## Django 4.2 阶段2验收（3.2.25 -> 4.2.30）

### 验收环境
- 容器 `tdyw-test`（镜像 `tdyw:django42-stage2`，healthy）
- 数据库 `tdyw-db-test`（mariadb 10.8.2）

### 实际依赖版本
- Django 4.2.30, asgiref 3.11.1, channels 4.3.2（验收中从 3.0.5 升级）, channels_redis 4.3.0, daphne 4.2.2
- celery 5.2.7, django_celery_beat 2.9.0, django_celery_results 2.4.0, django_redis 5.4.0, mysqlclient 2.1.1

### 检查命令结果（全 exit 0）
1. `python manage.py check` → System check identified no issues
2. `python -Wa manage.py check` → System check identified no issues（**无 RemovedInDjango50Warning**）
3. `python manage.py makemigrations --check --dry-run` → No changes detected
4. `python manage.py migrate --plan` → No planned migration operations

### Supervisor（12/12 RUNNING）
nginx/redis/spug-api/spug-api-upload/spug-ws/spug-worker/spug-celery-batch/merge/cleanup/default/radio-license/beat 全部 RUNNING

### Celery（5 nodes online）
cleanup/radio-license/default/batch/merge 全部 pong

### 核心 API 回归（全部 HTTP 200）
登录(self/role/tenant/user)/设备/干扰(列表+统计+导出10KB)/运行日志/日检查单/系统升级/审计日志(16617条+导出1.4MB)/无线电执照(列表+角标+提醒)/首页统计/系统设置(about确认django_version=4.2.30)/资料库(folder/disk/health/transfers)/退出

### 资料库上传/下载/删除（全 HTTP 200）
普通上传→列表确认→下载内容正确→删除→列表空，链路完整

### WebSocket 握手
直连9002 + 经nginx80 均返回 `HTTP/1.1 101 Switching Protocols`

### 验收中修复的问题：Channels 2.x -> 4.x WebSocket 兼容（3 个文件）

**根因**：Channels 2.x 在 `__init__` 时设置 `self.scope`，3.x/4.x 改为在 `__call__` 时设置；且 Channels 4.x 的 `URLRouter` 不再自动对 consumer 类做 `guarantee_single_callable` 转换。

**修复 1 — `consumer/consumers.py`**：
- `PubSubConsumer` 和 `ComConsumer` 的 `__init__` 中访问 `self.scope` 的逻辑移到 `init()` 方法（`BaseConsumer.connect()` 验证用户后调用的钩子）
- `disconnect()` 加 `hasattr` 保护（连接失败时 rds/p 可能未初始化）

**修复 2 — `consumer/routing.py`**：
- consumer 类注册改为 `PubSubConsumer.as_asgi()` / `ComConsumer.as_asgi()`
- Channels 4.x 要求 consumer 类通过 `as_asgi()` 转为 single-callable ASGI 应用

**修复 3 — `spug/asgi.py`**：
- Channels 4.x 移除了 `get_default_application()`，改为 `from spug.routing import application`

**修复 4 — `requirements.txt`**：
- channels/channels_redis/daphne 从 `>=3,<4` 改为 `>=4,<5`（阶段2计划只升到 3.x，但 3.x 与 asgiref 3.11 有兼容 bug，必须升到 4.x）

**关键排查经验**：
- `is_double_callable()` 对"类"无条件返回 True（`inspect.isclass(application)`），导致 Channels 4.x URLRouter `await application(scope,...)` 把 consumer 类当 double-callable 误处理
- `as_asgi()` 返回 coroutine function（single-callable），绕过 `is_double_callable` 误判
- 错误演进：`self.scope AttributeError`（__init__ 问题）→ `AsyncConsumer.__call__ missing 'send'`（3.x 兼容 bug）→ `can't be used in await`（未用 as_asgi）→ `101 Switching Protocols`（修复成功）

### 遗留问题（非 Django 4.2 阻断项）
- `celery_cleanup_error.log` 有 "Received unregistered task"：settings.py 的 CELERY_TASK_ROUTES/CELERY_BEAT_SCHEDULE 任务名（`apps.document.tasks.cleanup.retry_clean_pending_files`、`apps.document.tasks.cleanup_old_chunks`）与重构后实际模块路径（`apps.document.tasks.cleanup.pending_files.retry_clean_pending_files`、`apps.document.tasks.cleanup.chunks.cleanup_old_chunks`）不一致，属资料库任务模块重构遗留 bug，非升级引入
- ws_err.log 历史 `ValueError: No route found for path ''`：daphne 对空路径探测请求的响应，非升级问题

### 镜像重建提醒
当前容器内 Channels 4.x 是 pip 临时安装的，镜像 `tdyw:django42-stage2` 仍是 Channels 3.x。需用更新后的 requirements.txt 重新构建镜像才能固化。

### 阶段2成功标准达成情况
✅ Django 4.2.30 / check 无错误 / -Wa 无 Django4 阻断 warning / makemigrations No changes / migrate plan 无非预期迁移 / 镜像可启动 / 12 进程 RUNNING / 登录和主要页面可访问 / Celery worker+beat 正常 / WebSocket 正常 / 文件上传/下载/删除正常 / 导出正常

## 资料库模块前后端 4 项修复

修复资料库搜索租户隔离、合并分页 has_more、前端 API 常量路径、clearCompletedTransfers 语义错误。

### 1. 私有空间搜索漏租户过滤（安全）
- 位置：`spug_api/apps/document/views/search.py` FolderSearchView.get
- 根因：folders_query 在非公共空间调了 apply_tenant_filter，files_query 没调，folder_id=None 的根目录文件可能跨租户泄露
- 修复：files_query 构造后、count 前，`if not form.is_public: files_query = apply_tenant_filter(files_query, request.user, strict_mode=True)`
- 注意：search.py 自身的 has_more 用 `max(total_folders, total_files)` 是正确的（folders/files 独立切片，max 表示"任一列表还有更多"），与 folder/views.py 的合并分页语义不同，无需改

### 2. 文件夹/文件合并分页 has_more 不准
- 位置：`spug_api/apps/document/views/folder/views.py` _get_root_contents / _get_folder_contents
- 根因：合并分页（文件夹在前文件在后）用 `max(total_folders, total_files)` 判断 has_more，应按两者总数
- 修复：两处引入 `total_items = total_folders + total_files`，has_more 改为 `(offset + page_size) < total_items`

### 3. 前端 API 常量路径与后端不对齐
- 位置：`spug_web/src/pages/document/stores/constants/api.js`
- 先搜索确认 FILE_DOWNLOAD/FILE_PREVIEW/SEARCH/DISK_INFO/FOLDER_TREE 在 API_ENDPOINTS.XXX 形式下均无调用方
- 修正：FILE_DOWNLOAD `/file/download/`→`/download/`；FILE_PREVIEW `/file/preview/`→`/preview/`；SEARCH `/search/`→`/folder/search/`；DISK_INFO `/disk/`→`/disk_usage/`
- FOLDER_TREE：后端无此路由且无调用方，保留但加 `@deprecated` 注释避免误导

### 4. clearCompletedTransfers 语义错误
- 位置：`spug_web/src/pages/document/stores/upload/core/transfer.js`
- 根因：调用 TRANSFERS_BATCH_CANCEL 且不传 transfer_ids，会取消所有传输任务，与"清理已完成"语义不符
- 经搜索确认全项目无调用方
- 修复：改为安全 no-op + console.warn + 注释说明后端暂无专用接口，如需清理应用 batchDeleteTransfers(已完成ID列表)

### 验证
- read_lints 0 错误（4 个改动文件）
- 后端 `python -m compileall apps/document` exit 0（tdyw-test 容器，search.py + folder/views.py 均编译通过）
- 前端 `npm run build` exit 0（构建成功，仅有 runlog/Table.js、useColumns.js 等无关文件的预存 lint 警告）
- 无现成资料库搜索/分页测试可跑（前端测试仅上传相关，后端 apps/document 无 test 文件）

### 关键经验
- 合并分页的 has_more 必须按总数判断，独立切片分页用 max 才对——两种语义不能混用
- 改 API 常量前必须先 grep 调用方，避免误删/误改在用导出；本次确认 5 个常量均无调用才改路径
- 未被调用的方法误用危险接口（batch cancel 不传 ids）是隐性 bug，宁可 no-op 也别留危险实现

## 资料库传输列表状态机修复（7 项问题）

依据《资料库传输状态机修复建议.md》修复前端状态集合混用、cancelItem 绕过状态机、后端同步重复、_directMerge 绕过状态机、批量恢复漏任务、后端统计不准、清空接口语义 7 项问题。

### 修改文件（17 个）

**前端核心（10 个）**：
- `upload-core-constants.js`：新增 `SLOT_OCCUPYING_STATUSES`/`PAUSEABLE_STATUSES`/`DISPLAY_UPLOADING_STATUSES` 三个语义化常量
- `UploadStateMachine.js`：新增 `RETRY_MERGE` 事件（error/waiting → merging）；移除 entry 钩子中的 `updateTransferStatus` 调用（消除重复同步）
- `StateMachineManager.js`：`batchPause` 改用 `PAUSEABLE_STATUSES`；`batchResume` 改用 `SLOT_OCCUPYING_STATUSES` 计数
- `ItemOperationController.js`：`cancelItem` 改为通过 `transition('CANCEL')` 进入 cancelled 终态（与批量取消一致）；`_directMerge` 改为通过 `transition('RETRY_MERGE')` 进入 merging
- `QueueOperationController.js`：`removeAll` 改用 `DISPLAY_UPLOADING_STATUSES`（修复遗漏 waiting/paused 的 bug）
- `DebounceController.js`：`resumeAll` 新增 `startWaiting()` 调用覆盖懒创建状态机的 waiting 任务；改用 `SLOT_OCCUPYING_STATUSES`
- `StateChangeHandler.js`：`handleMergingState` 传入 event 参数，`RETRY_MERGE` 路径跳过 `mergeChunks`（_directMerge 已处理）
- `index.js`：`uploadingItems`/`activeCount` 改用 `DISPLAY_UPLOADING_STATUSES`（修复 activeCount 遗漏 waiting 的 bug）
- `sync/StatusSynchronizer.js`：移除 `@action` 装饰器（jest 环境不支持 decorator 语法，导致测试无法加载）
- `StoreEventAdapter.js`：`handleTransferStatusUpdate` 添加降级注释

**前端 UI（4 个）**：
- `TransferItem.js`：`canPause` 改用 `PAUSEABLE_STATUSES`；`canRemove` 改用 `TERMINAL_STATUSES`
- `TransferList.js`：`isUploadingStatus` 改用 `DISPLAY_UPLOADING_STATUSES`
- `VirtualTransferList.js`：`pauseableCount` 改用 `PAUSEABLE_STATUSES`（修复误将 merging 计为可暂停的 bug）
- `UploadPanel.js`：`componentWillUnmount` 改用 `PAUSEABLE_STATUSES`（修复尝试暂停 merging 的 bug）；MiniBar 改用 `DISPLAY_UPLOADING_STATUSES`/`TERMINAL_STATUSES`

**协调器（2 个）**：
- `UploadCoordinator.js`：`startWaiting` 改用 `SLOT_OCCUPYING_STATUSES`
- `RecoveryCoordinator.js`：`countByStates` 改用 `SLOT_OCCUPYING_STATUSES`

**后端（1 个）**：
- `batch.py`：`TransferBatchPauseView`/`TransferBatchResumeView` 返回 `updated_ids`/`already_ids`/`skipped_ids`，区分"新更新"和"原本已在此状态"

**前端 Store（1 个）**：
- `transfer.js`：`batchPauseTransfers`/`batchResumeTransfers` 使用 `already_ids` 展示更准确的统计文案

**测试（2 个）**：
- `StateMachineManager.test.js`：修复 4 个 pre-existing 失败测试（mock item 添加 `totalChunks: 5`，终态隔离）；新增 4 个测试（RETRY_MERGE、单个/批量取消一致性、批量取消后状态）
- `StatusSynchronizer.test.js`：新增 7 个测试（error 终态同步、cancelled 阻止 merging 回退、error 阻止 uploading 回退、waiting 不同步、去重、paused/merging 同步）

### 验证结果
- 单元测试：4 suite / 58 test 全部通过
- 前端构建：`npm run build` exit 0（仅有无关文件的预存 lint 警告）
- read_lints：所有改动文件 0 错误

### 关键设计决策
1. **cancelItem 改为"取消并保留记录"**：与批量取消语义一致，transition('CANCEL') 进入 cancelled 终态，用户可通过"删除"按钮永久移除
2. **RETRY_MERGE 事件**：error/waiting → merging 的快捷路径，避免重传分片；StateChangeHandler 检测到 RETRY_MERGE 事件时跳过 mergeChunks（_directMerge 已调 DIRECT_MERGE 接口）
3. **后端同步唯一链路**：StateChangeHandler → StatusSynchronizer.syncStateToBackend()，移除 entry 钩子中的 actions.updateTransferStatus() 调用
4. **SLOT_OCCUPYING_STATUSES 不含 merging**：merging 是后端 Celery 任务，前端只轮询，不占前端网络/CPU 槽位
5. **PAUSEABLE_STATUSES 不含 merging/paused**：merging 不可暂停（状态机无 PAUSE 转换），paused 不可重复暂停

### 待确认的产品语义
- cancelItem 改为保留记录后，用户需要点"删除"才能永久移除——如果产品希望取消后直接消失，需改回 removeItem 并区分文案

## 资料库传输列表状态机修复（第二轮强化）

承接第一轮修复，按用户更严格的产品语义要求做 4 项强化。

### 修改文件（5 个）
1. `controls/ItemOperationController.js` — `_directMerge` 重构：状态机重建失败或不可 RETRY_MERGE 时不再悄悄直接写 `item.status='merging'`，而是返回 false 让调用方走 RESUME 降级；先验证状态机可用再调 DIRECT_MERGE 接口（避免后端已开始合并但前端状态机仍 error/waiting）
2. `controls/DebounceController.js` — `_doResumeAll` 重构：改为按队列 item 扫描 paused 任务（不再只遍历 machines），paused 无状态机时先 ensureStateMachine 再 transition('RESUME')；waiting 交给 startWaiting 调度
3. `StoreEventAdapter.js` — `handleTransferStatusUpdate` 改为委托 statusSynchronizer.shouldSync/markSynced 做去重，与权威链路共享 _lastSyncStatusMap
4. `sync/StatusSynchronizer.js` — 抽出 `shouldSync(transferId, backendStatus)` 和 `markSynced(transferId, backendStatus)` 公共方法，供 StoreEventAdapter 共用
5. `index.js` — `_initEventAdapter` 传入 `statusSynchronizer` 引用

### 测试（2 个文件，新增 8 个测试）
- `StateMachineManager.test.js`：新增 4 个测试（批量恢复跳过终态/merging、completed 不可回滚、merging 不可暂停、cancelled 不可 RETRY_MERGE）
- `StatusSynchronizer.test.js`：新增 4 个测试（shouldSync 首次/重复/状态变化、StoreEventAdapter 共用去重模拟）

### 验证结果
- 单元测试：4 suite / **66 test 全部通过**（上轮 58，本轮 +8）
- 前端构建：`npm run build` exit 0
- read_lints：所有改动文件 0 错误

### 关键设计决策
1. **_directMerge 不再降级直接写 merging**：状态机不可用时返回 false → 调用方 resumeItem 继续走 RESUME → 状态机 ERROR → onErrorEntry → 用户看到"合并失败，请重试"。避免 UI 显示 merging 但状态机仍在 error 的分裂
2. **批量恢复按队列 item 扫描**：pausedItems 从 `queue.filter(status==='paused')` 获取，不依赖 machines Map；paused 无状态机时 ensureStateMachine 重建，重建后为 waiting 则交给 startWaiting
3. **StoreEventAdapter 去重共用**：shouldSync/markSynced 是 StatusSynchronizer 的公共方法，StoreEventAdapter 调用前先检查，避免同一状态通过两条链路重复请求后端
4. **clearCompletedTransfers 已是 no-op**：经搜索确认全项目无调用方，UploadPanel 直接用 batchDeleteTransfers 清空，语义正确

## 操作审计模块余下修复（依据《操作审计模块修复建议.md》）

承接前次会话已完成的 P0 三项（业务失败被审计为成功、DELETE query 参数提取、POST+action=delete 映射），本次修复 P1/P2 余下 7 项。

### 修改文件（8 个，+109/-38 行）

**后端（5 个）**：
- `apps/logs/audit.py`：TARGET_MAP 补全 3 项 — `/account/tenant`→tenant、`/radio-license/`→radio_license、`/logs/`→audit。注意 `/account/tenant` 必须放在 `/account/user` 之前匹配（`resolve_target` 是首个 prefix 命中即返回）
- `apps/logs/views.py`：AuditLogView.get 增加 page/page_size 边界保护（page>=1，10<=page_size<=100），避免负数切片和超大分页
- `apps/logs/middleware.py`：敏感字段脱敏改为关键词黑名单（SENSITIVE_KEYWORDS = password/token/secret/key/private/credential），新增 `_is_sensitive_field()` 函数。覆盖 old_password/new_password/private_key/public_key/api_key/spug_push_key/wx_token 等所有衍生命名
- `apps/logs/models.py`：AuditLog.Meta 增加 5 个索引（tenant_id+-id、tenant_id+created_at、action、target_type、username）
- `apps/logs/migrations/0002_add_audit_indexes.py`：新增 migration（Django 4.2.30 生成，已 apply 到数据库）
- `apps/account/views.py`：handle_login_record 中登录失败租户归属策略加注释说明（已存在用户→该用户租户；未知用户→default 全局归档仅超管可见）

**前端（3 个）**：
- `pages/system/audit/index.js`：顶部"操作类型/对象类型"由 Input 改为 Select 枚举（复用 store.actionOptions/targetTypeOptions），打印按钮文案改为"打印当前页"，新增"重置"按钮，非超管筛选"登录"动作时显示提示"未知账号的登录失败仅超管可见"
- `pages/system/audit/Table.js`：移除工具栏中重复的操作类型/对象类型 Select（与顶部筛选重复），TARGET_TYPE_MAP 补全 tenant/radio_license/audit，移除冗余 Select/Option 导入
- `pages/system/audit/store.js`：新增 resetFilters() 方法

### 验证（41 项全 PASS）
- 后端 py_compile COMPILE_OK（tdyw-test 容器）
- read_lints 0 错误（8 个改动文件）
- 回归脚本 verify_audit_fixes.py 41 项全 PASS：P1-4 TARGET_MAP 8 项、P1-3 action 映射 9 项、P1-6 敏感字段 26 项、P1-5 分页钳制 5 项、P2-10 索引存在 2 项（含数据库 information_schema.STATISTICS 实际索引验证）
- migration 已 apply：`Applying logs.0002_add_audit_indexes... OK`

### 已知折中
- `keyword` 字段因包含 `key` 被关键词黑名单误判为敏感。经搜索确认全项目无 `Argument('keyword'` 写操作表单定义（keyword 仅用于 GET 查询参数），不会进入审计 detail，折中安全。若未来表单新增 keyword 字段需审计，可在 `_is_sensitive_field` 加白名单。

### 关键经验
- `resolve_target` 是首个 prefix 命中即返回，`/account/tenant` 必须排在 `/account/user` 之前，否则会被吞掉匹配（实测 `/account/tenant/list/` 仍解析为 tenant）
- 敏感字段用关键词黑名单比固定字段名集合覆盖更广，但会误伤含 `key` 的普通字段（如 keyword），需配合白名单或确认无业务字段冲突
- Django 索引 migration 用 `models.Index(fields=['tenant_id', '-id'], name='...')`，`-id` 表示降序索引，MySQL 8.0+ 支持
- 验证数据库索引存在时，PostgreSQL 用 `pg_indexes`，MySQL 用 `information_schema.STATISTICS`（本项目是 MariaDB 10.8）
- PowerShell 下 `docker exec bash -c "...$?"` 会被 PowerShell 先解释 `$?` 为 True，需用单引号包裹 bash 脚本或把退出码写入日志文件再读

## 操作审计模块 — 长期归档策略（保留 180 天）

承接操作审计模块修复的未做项，实现审计日志 180 天保留 + 自动清理。

### 修改文件（4 个）
- `apps/logs/tasks.py`（新增）：`cleanup_old_audit_logs` shared_task
- `apps/logs/celery_beat_schedule.py`（新增）：`LOGS_BEAT_SCHEDULE`，每天 04:00，days=180
- `spug/settings.py`：`CELERY_TASK_ROUTES` 加 route → default 队列；导入合并 `LOGS_BEAT_SCHEDULE`

### 设计要点
- AuditLog.created_at 是 CharField 存 "YYYY-MM-DD HH:MM:SS"，字典序=时间序，直接 `created_at__lt=cutoff_str` 字符串比较
- MIN_RETENTION_DAYS=30 钳制 days 下限，防误删近期数据
- DELETE_BATCH_SIZE=5000 分批删除，避免长事务/主从延迟
- dry_run 参数仅统计不删除
- 队列用 default（非 document/radio_license 专用）
- 执行时间 04:00 避开 document 模块 02:00/03:00/05:00

### 验证（18 项全 PASS）
- 任务 name 正确、beat schedule 注册、task route 配置
- dry_run 不删数据（before=after=17801）
- days=1 钳制为 30
- 真实清理：200天前测试数据被删（deleted=1），当前数据保留
- `celery inspect registered` 确认 worker 加载任务
- beat + default worker 重启后 RUNNING

### 关键经验
- `celery -A spug inspect registered` 可确认 worker 是否加载了某任务（grep 过滤）
- `task.apply(kwargs={...}).get()` 在当前进程同步执行，不需 worker 在线，适合验证脚本
- Celery `autodiscover_tasks()` 自动发现 `apps/<app>/tasks.py`（非 tasks/ 目录），logs/tasks.py 无需显式导入
- 重启 beat 让新 schedule 生效：`supervisorctl restart spug-celery-beat spug-celery-default`
- PowerShell 下 `docker exec ... python -c "多行代码"` 会被 PowerShell 解析报错，需写成脚本文件再执行

## 运行日志模块余下修复（依据《运行日志模块修复建议.md》）

承接前次会话已完成的 P0（鉴权补齐、附件路径安全、日期参数契约、附件清理）和 P1（动态编辑权限统一、删除动态清附件、状态标签权限控制、操作列权限），本次修复 P2 余下项。

### 修改文件（3 个，+6/-38 行）

1. `spug_web/src/pages/runlog/Form.js` — 删除 fetchUpdates 和 useEffect 中 11 处调试 `console.log`（含打印整个响应、update attachments 等），保留必要的 `console.error`
2. `spug_api/apps/runlog/views.py`：
   - RunLogRepairView 中 3 处 `print(...)` 改为 `logger.info/warning`（修复动态 tenant_id、修复 update_count、修复失败）
   - 删除 RunLogView 类中未被路由调用的 `get_detail` 死代码方法（URL 实际用 urls.py 中的 RunLogDetailView，避免两份重复逻辑导致后续漏改）
3. `spug_web/src/pages/runlog/EventTypeModal.js` — 弹窗标题下增加副标题"事件类型为全局共享配置，仅超级管理员可管理"，与后端 is_superuser 校验保持一致（事件类型权限未进入角色权限树，保持现状但 UI 明确）

### 验证
- read_lints：3 个改动文件 0 错误
- 后端 `python -m py_compile views.py` COMPILE_OK（tdyw-test 容器）
- Django 模块导入校验：`from apps.runlog import views, urls` IMPORT_OK（PYTHONPATH=/data/spug/spug_api，DJANGO_SETTINGS_MODULE=spug.settings）

### 跳过项
- #12 ForeignKey 长期建议（runlog_id IntegerField → ForeignKey）：需单独迁移方案，不与本轮修复混做

### 关键经验
- 容器内 settings 模块是 `spug.settings`（非 `spug_api.settings`），`spug` 包在 `/data/spug/spug_api/spug/`，需 `PYTHONPATH=/data/spug/spug_api`
- py_compile 只校验语法，模块导入校验额外覆盖装饰器解析、`from .views import *`、urlpatterns 构建
- PowerShell 下 `docker exec ... python -c "from ..."` 会被 PowerShell 把 `from` 当关键字报错，改用 heredoc 写临时脚本文件更稳妥

## 资料库上传模块过度设计评估（用户问"他说的对吗"）

用户反馈"UploadCoreStore 组合了 14 个子模块，大量方法只是代理转发，调用链太长"。我逐文件核对后的结论：**基本对，但需区分"真过度"和"合理拆分"**。

### 真过度的部分（应收敛）
1. **DisplayCoordinator 是空壳**（25 行）：核心方法 `replenish()` 只有 1 行 `this.core.uploadCoordinator.startWaiting()`。而 `UploadCoordinator.processPending()` 也是 1 行 `this.startWaiting()`。导致"启动等待任务"有 3 条等价路径（`replenishDisplayQueue` / `processPendingUploads` / `startWaitingTasks`）全部汇到 startWaiting，调用方不知该调哪个。
2. **EventBus + StoreEventAdapter 是"未完成的解耦"**：`useDecoupledStateMachine = false`（解耦版状态机从未启用），但 `_initEventAdapter()` 仍被调用并订阅 14 种事件。`UploadStateMachine.js:44` 仍 `createActions(uploadId)`，entry/exit 钩子通过 `this.actions.*` emit 事件（29 处调用），被 `StoreEventAdapter.handleX` 收到后写 `queueStore.updateUploadItem`。结果同一份 `item.status` 被两条路径同时写：
   - 路径 A：`stateMachineManager.addGlobalListener` → `StateChangeHandler.handle` → 业务逻辑
   - 路径 B：`stateMachine.entry/exit` → `actions.updateItem` → `EventBus.emit` → `StoreEventAdapter.handleStatusChange` → `queueStore.updateUploadItem`
   代码注释里到处可见"【7.1 状态机唯一入口】不写 status，由状态机 onCompletedEntry 写"——这种注释就是在两条路径间做"分工防冲突"，是过度设计的典型症状。StoreEventAdapter 自身注释也承认 `TRANSFER_STATUS_UPDATE` 事件已不再触发，整个 adapter 半死不活。
3. **index.js 的大量纯转发代理**：`323:486` 一段约 20 个方法都是单行转发，其中 `transferStore` 5 个方法（`465:469`）尤其可疑，外部可直接用 `core.transferStore.xxx`。

### 合理的部分（不要动）
| 文件 | 行数 | 评价 |
|---|---|---|
| UploadCoordinator | 254 | startWaiting/并发控制/入队 实质逻辑 |
| DebounceController | 305 | pauseAll/resumeAll 防抖+真实执行+后端同步 |
| ItemOperationController | 478 | _directMerge/分片检测/降级路径 |
| QueueOperationController | 130 | cancelAll/removeAll 版本号+uniqueKey 清理 |
| StateChangeHandler | 376 | 状态变更分发枢纽 |
| UploadLifecycle | 150 | MD5/完成/错误回调 |
| StatusSynchronizer | 145 | 前后端同步+去重 |
| NetworkLifecycle | 70 | 网络监听，小而专 |

REFACTOR_PLAN.md 记录原 index.js 1630 行，拆分方向本身没错，这些文件配得上独立。

### 收敛建议（按 ROI 排序，未动手）
1. **删 DisplayCoordinator**（最高 ROI）：`index.js:351-353` replenishDisplayQueue + `UploadLifecycle.js:33,56` + `ItemOperationController.js:374,448` 改为直接 `uploadCoordinator.startWaiting()`，删 1 个文件 + 消除 3 条等价路径
2. **二选一**：要么真正启用解耦版状态机，要么删 EventBus/StoreEventAdapter/actions 的 emit 部分。鉴于 StateChangeHandler 已是稳定权威路径，建议后者
3. **删 index.js 纯转发代理**：transferStore 5 个方法让外部直接用 core.transferStore.xxx

**不要做**的大动作：把所有 coordinator/lifecycle/controller 全合回 index.js——那是回到 1630 行的旧坑，且会丢掉 __tests__/ 15 个测试文件的可测性收益。

### 关键经验
- "未完成的解耦"比没有解耦更糟：`useDecoupledStateMachine=false` 但 adapter 仍订阅事件，留下平行更新路径
- 空壳类（25 行单行代理）+ 等价路径（3 条都通向同一方法）是判断过度拆分的两条铁证
- 评估"过度设计"不能笼统说"分层太多"，要逐文件看：行数 + 是否有实质职责 + 是否产生等价路径

## 删除空壳 DisplayCoordinator（上传模块过度设计收敛建议第 1 步）

承接上传模块过度设计评估，执行 ROI 最高的第 1 步：删除 25 行空壳 DisplayCoordinator，消除"启动等待任务"的 3 条等价路径。

### 改动（5 个文件，删 1 个文件）
1. **删** `coordinators/DisplayCoordinator.js`（25 行空壳，replenish() 内部只有 1 行 `this.core.uploadCoordinator.startWaiting()`）
2. `coordinators/index.js`：移除 `export { DisplayCoordinator }`
3. `core/index.js`（4 处）：移除 import / `displayCoordinator` 字段 / `_initCoordinators` 实例化 / `replenishDisplayQueue()` 方法体改为 `this.uploadCoordinator?.startWaiting()`（保留方法名兼容 `UploadPanel.js:143` 调用）
4. `lifecycle/UploadLifecycle.js`（2 处）：`onCompleted`/`onError` 中删除冗余的 `displayCoordinator.replenish()` 调用（两处后面都紧跟 `uploadCoordinator.startWaiting()`，replenish 内部也是调 startWaiting，属于双重调用）
5. `controls/ItemOperationController.js`（2 处）：`cancelItem`/`removeItem` 末尾 `displayCoordinator.replenish()` 改为 `uploadCoordinator.startWaiting()`

### 验证
- read_lints：0 error，仅 3 个 pre-existing HINT（StateChangeHandler/StateMachineManager 未使用变量，与本次无关）
- 全局搜索 `DisplayCoordinator|displayCoordinator`：0 残留
- jest 测试（`react-app-rewired test`）：14 suite / 220 test，8 suite passed / 183 test passed，6 suite failed / 37 test failed
- **37 个失败全部是 pre-existing**，证据：
  1. `UploadStateMachine.core.test.js` 期望"7个预定义状态"但代码有 8 个（cancelled 是后加的）——测试未同步
  2. 失败主题全是状态机内部行为（状态转换/守卫/updateContext/batchResume），与 DisplayCoordinator 职责无关
  3. __tests__ 目录无 displayCoordinator 引用
  4. 改动语义等价：replenish() 内部就是 startWaiting()

### 关键经验
- `DisplayCoordinator.replenish()` = `uploadCoordinator.startWaiting()`，导致"启动等待任务"有 3 条等价路径（replenishDisplayQueue / processPendingUploads / startWaitingTasks），全部汇到 startWaiting——删 1 个文件消除 3 条路径
- `UploadLifecycle.onCompleted/onError` 原来 replenish + startWaiting 双重调用（replenish 内部又调 startWaiting），startWaiting 是幂等的（基于 activeCount 计算槽位），双重调用无副作用但属冗余
- jest 测试必须用 `react-app-rewired test`（CRA 定制版），直接 `npx jest` 会因缺少 Babel 配置报 `Cannot use import statement outside a module`
- PowerShell 下 `CI=true` 要写成 `$env:CI='true'`，jest 才不会进入 watch 模式

## 删除解耦设施 EventBus/StoreEventAdapter/actions（方向B，上传模块过度设计收敛建议第 2 步）

承接第 1 步删 DisplayCoordinator，执行方向B：消除"item.status 被两条路径同时写"的平行更新问题。

### 背景
- `useDecoupledStateMachine = false`，解耦版状态机从未启用
- 但 `StoreEventAdapter` 仍订阅 14 种事件，`UploadStateMachine` entry/exit 通过 `this.actions.*` 向 EventBus emit
- 结果同一份 `item.status` 被 `StateChangeHandler`（权威）和 `StoreEventAdapter`（冗余）两条路径同时写
- 代码里到处是"【7.1 状态机唯一入口】不写 status，由 onCompletedEntry 写"这种防冲突注释

### 改动（删 3 个文件 + 改 4 个文件）

**删除 3 个文件（~640 行）**：
- `EventBus.js`（174 行）：发布订阅模式事件总线
- `StoreEventAdapter.js`（369 行）：14 种事件处理器，桥接 EventBus → Store
- `actions.js`（238 行）：action 创建器，通过 EventBus.emit 通知

**改造 `UploadStateMachine.js`**（核心）：
- 删 `import { createActions }`，`this.actions = null`
- 新增 3 个 helper：`updateItem(updates)` / `cleanupUploadResources()` / `cleanupAllResources()`
- entry/exit 钩子里 `this.actions.updateItem(...)` → `this.updateItem(...)`
- `this.actions.cleanupUploadResources()` → `this.cleanupUploadResources()`
- `this.actions.cleanupAllResources()` → `this.cleanupAllResources()`
- 删除死代码：`this.actions.startMD5Calculation()` / `cancelMD5Calculation()`（md5Store 根本没这两个方法，被 `?.` 吞掉）
- transitions 里所有 `action: this.actions.onCancel/onNormalUploadComplete/onResumeToCalculating/onResumeToUploading` 全部删除（entry 钩子已处理副作用）
- `onCompletedEntry` 补 `completedAt: Date.now()`（原由 adapter handleNormalUploadComplete 写）
- `onResumeToWaitingAction`/`onRetryAction` 改为直接 `this.updateItem`，保留原 adapter 副作用语义
- transition 异常分支的 `this.actions.reportError` 改为直接 `this.updateItem({status:'error', error, canAbort:false})`

**改造 `index.js`**：
- 删 `import { globalEventBus }` / `StoreEventAdapter` / `createActions, UploadEvents`
- 删 `storeEventAdapter` 字段
- `_initEventAdapter()` 改为 no-op
- `_initGlobalListeners()` 移除 adapter 调用
- `destroy()` 移除 adapter 销毁
- export 块移除 globalEventBus/StoreEventAdapter/createActions/UploadEvents

**改造测试文件**：
- `_gatewayEnv.js`：重写，移除 StoreEventAdapter/EventBus 依赖，仅保留 manager + mock queueStore（teardownGatewayEnv 保留 adapter 参数仅为向后兼容）
- `concurrencySlot.test.js`：删 `import { StoreEventAdapter }` / `import { globalEventBus }`，setup 移除 adapter 实例化，teardown 移除 `adapter.destroy()` / `globalEventBus.clear()`

### 验证结果
- read_lints：0 error，仅 3 个 pre-existing HINT（与本次无关）
- 全局搜索 `from './EventBus'|from './StoreEventAdapter'|from './actions'`：0 真实引用（navigation/index.js 为字符串子串误报）
- jest 测试：14 suite / 220 test，8 suite passed / 183 test passed，6 suite failed / 37 test failed
- **与方向B 改造前完全一致**（改造前也是 8/6 + 183/37），说明改造未引入任何新失败
- 37 个失败全部是 pre-existing：状态机内部行为测试（状态转换规则/守卫/updateContext/batchResume），测试 mock 与代码不同步（如期望 7 个状态但代码有 8 个 cancelled）

### 关键设计决策
1. **transitions 的 action 全删，副作用统一到 entry**：原 `action: this.actions.onCancel` emit 事件 → adapter handleCancel 写 item，但 `onCancelledEntry` 已写同样字段，action 是冗余
2. **onCompletedEntry 补 completedAt**：原由 adapter handleNormalUploadComplete 写，现在 entry 直接写
3. **onResumeToWaitingAction 用微任务延迟写 error**：原 adapter handleResumeToWaiting 在 entry 后执行，现在需延迟避免被 onWaitingEntry 的 `error:null` 覆盖
4. **死代码删除**：`startMD5Calculation`/`cancelMD5Calculation` 在 md5Store 不存在，实际 MD5 由 StateChangeHandler → UploadLifecycle.onCalculating 触发
5. **_gatewayEnv.js teardownGatewayEnv 保留 adapter 参数**：3 个 stateMachineGateway.*.test.js 调用 `teardownGatewayEnv(adapter, manager)`，保留参数避免破坏调用方

### 关键经验
- "未完成的解耦"比没有解耦更糟：`useDecoupledStateMachine=false` 但 adapter 仍订阅，留下平行更新路径
- 删解耦设施时，entry/exit 钩子的 `this.actions.updateItem` 改为 `this.context.queueStore.updateUploadItem` 是语义等价替换
- transitions 的 `action` 字段如果只是 emit 事件给 adapter，而 entry 钩子已做同样的事，这个 action 就是冗余，可直接删
- 测试文件用 StoreEventAdapter 桥接 EventBus → mockQueueStore 的，删 adapter 后 mockQueueStore.updateUploadItem 仍能收到 entry/exit 的直接调用，测试逻辑无需大改
- jest 测试结果与改造前完全一致是验证"语义等价替换"成功的最强证据

## 删除 index.js 死代码代理方法（方向B，上传模块过度设计收敛建议第 3 步）

承接第 1 步删 DisplayCoordinator、第 2 步删解耦设施，执行第 3 步：删除 index.js 中 0 调用方的纯转发代理方法。

### 背景
- UI 层已混合使用两种模式：既通过代理调 `uploadCoreStore.handleFileSelect()`，又直接访问 `uploadCoreStore.transferStore.batchDeleteTransfers()`
- 大量代理方法是纯死代码（0 外部调用 + 0 内部 this.core.xxx 调用）

### 改动（仅 index.js，删 13 个方法）

**删除的死代码代理**（0 调用方）：
1. `fetchTransfers(isPublic)` — transferStore 代理
2. `createTransfer(data)` — transferStore 代理
3. `updateTransferStatus(transferId, status)` — transferStore 代理（内部直接用 this.core.transferStore.updateTransferStatus）
4. `cancelTransfer(transferId)` — transferStore 代理
5. `deleteTransfer(transferId)` — transferStore 代理
6. `syncTransferStatus(isPublic)` — statusSynchronizer 代理
7. `mapBackendStatus(backendStatus)` — statusSynchronizer 代理（StatusSynchronizer 内部用自己的 this.mapBackendStatus）
8. `cleanupMD5WorkerPool()` — md5Store 代理（与 destroy 里的 this.md5Store.terminateAll 重复）
9. `startWaitingTasks()` — uploadCoordinator 代理
10. `processPendingUploads()` — uploadCoordinator 代理
11. `schedulePendingUploadsRecovery()` — recoveryCoordinator 代理
12. `uploadSingleFile(file, folderId, existingUploadId, isPublic)` — fileUploadCoordinator 代理
13. `processUploadQueue(files, folderId)` — fileUploadCoordinator 代理

**保留的 Facade 代理**（有真实外部调用方）：
- `handleFileSelect` — document/index.js:127 调用
- `handleFolderSelect` — document/index.js:138 调用
- `replaceFileAndResume` — UploadPanel.js:97 调用
- `pauseAll`/`resumeAll` — UploadPanel.js + KeyboardShortcuts.js 调用
- `pauseItem`/`resumeItem`/`cancelItem`/`removeItem` — UploadPanel.js 调用
- `replenishDisplayQueue` — UploadPanel.js:143 调用

### 验证
- read_lints：0 error
- 全局搜索 13 个方法名的 `uploadCoreStore.xxx` 调用：0 残留
- jest：8 suite passed / 6 failed, 183 passed / 37 failed——与前两步完全一致

### 关键经验
- 删代理方法前必须搜调用方：`uploadCoreStore\.方法名` 和 `this\.core\.方法名` 两种模式都要搜
- UI 层混合使用代理和直接访问子 Store时，纯转发代理是死代码
- Facade 模式的价值在于"有外部调用方的统一接口"，不在于"给所有子 Store 方法都包一层代理"
- 区分"有价值的 Facade 代理"（有外部调用方）和"无价值的纯转发"（0 调用方）是关键

## fileUpload.js 7.1 改造（让"状态机唯一入口"边界真正成立）

承接前三步，修复用户指出的核心问题：fileUpload.js 直接写核心字段（status/canAbort/completedAt 等），违反"状态字段只能由状态机 entry/exit 投影"边界。

### 背景
用户确认架构合理后提出关键边界：
- 核心字段（status/canAbort/isPausedByUser/isCancelledByUser/completedAt）只能由状态机 entry/exit 写
- StateChangeHandler 只处理业务副作用（启动 MD5/上传/合并/同步后端/调度下一个）
- 进度/速度/已上传大小等非核心字段可由上传回调更新

扫描发现 fileUpload.js 是最大破坏者：
1. uploadFileToFolder（死代码）：8 处违规写入核心字段，但 0 调用方
2. uploadFileNormal（主路径）：基本已完成 7.1 改造，仅 completedAt 重复写入

### 改动（1 个文件）

**删除 `uploadFileToFolder` 方法**（~280 行，0 调用方）：
- 注释已标注"遗留死代码路径，无外部调用方"
- 文件夹上传主路径已由 FileUploadCoordinator._processBatch → 状态机 START 事件统一调度
- 该方法直接写 status:'uploading'/'completed'/'error'/'paused'，是最严重的违规

**修复 `uploadFileNormal` 的 completedAt 重复写入**：
- `:166` 删除 `completedAt: Date.now()`
- 原因：onCompletedEntry 在 `transition('UPLOAD_COMPLETE')` 后会写 `completedAt: Date.now()`
- 此处写 completedAt 是 entry 之前的时序，会形成双重写
- 改为仅清理 abortToken/abortController

**清理未使用的 import/函数**：
- 删除 `generateUploadId` import（原 uploadFileToFolder 专用）
- 删除 `buildFolderFileName` 函数（原 uploadFileToFolder 专用）

### 验证
- read_lints：0 error
- 扫描 fileUpload.js 的核心字段写入（status/canAbort/isPausedByUser/isCancelledByUser/completedAt）：0 残留
- jest：8 suite passed / 6 failed, 183 passed / 37 failed——与前四步完全一致

### 当前"状态机唯一入口"边界的真实状态

**✅ 主路径（StateChangeHandler 调用的）已守住**：
- `uploadFileNormal`（小文件上传）：错误抛 throw，成功触发 transition('UPLOAD_COMPLETE')，核心字段由 entry 写
- `uploadFileChunked`（分片上传）：已改造，错误抛 throw，成功触发 transition('UPLOAD_COMPLETE')

**🟡 已知例外点（有注释标注，批量/降级场景）**：
- `QueueOperationController.cancelAll`：批量取消直接写 status:'cancelled'（注释 TODO 7.1 例外点，原因是批量取消需立即中止所有请求 + 立即更新 UI）
- `ItemOperationController.cancelItem` 降级路径：状态机不可用时直接写 status:'cancelled'
- `ItemOperationController.abortUpload` 降级路径：状态机不可用时直接写 status:'error'
- `FileUploadCoordinator.replaceFileAndResume`：直接写 status:'waiting'（注释 TODO 7.1 例外点）

**🟢 入队初始化（可接受的灰色地带）**：
- `UploadCoordinator.processSingleFile` 入队：写 status:'waiting'（此时状态机还没创建）
- `FileUploadCoordinator._processBatch/_processUniform` 入队：同上

### 关键经验
- "状态机唯一入口"边界要分清主路径和例外点：主路径必须严格守住，例外点要有注释标注原因
- 死代码方法即使违规也应直接删除，不要花精力改造（0 调用方的方法改了也没用）
- onCompletedEntry 写 completedAt 后，业务代码不应再写，否则双重写
- 删方法后要检查未使用的 import 和辅助函数，一并清理

## 删除 cancelAll 死代码（消除"状态机唯一入口"最严重例外点）

承接 fileUpload.js 7.1 改造，用户指出"前端没有批量取消按钮"，确认 cancelAll 是死代码后删除。

### 背景
- 用户问"前端好像没有批量取消按钮" → 经查证确认 cancelAll 0 外部调用方
- cancelAll 仅在 destroy() 内部调用，但 destroy() 本身 0 调用方（单例从不销毁）
- cancelAll 的注释说"批量取消需立即更新 UI"——这个理由是假的，根本没有批量取消按钮
- cancelAll 直接写 status:'cancelled' 绕过状态机，是"状态机唯一入口"最严重的例外点

### 设计决策：不加批量取消按钮
参考成熟产品设计（百度网盘/阿里云盘/腾讯微云/OneDrive/Dropbox 均无批量取消按钮）：
1. "全部暂停"已覆盖"停止上传"需求，且可恢复
2. "取消"是低频操作，单个取消已够用
3. "清空已完成"（removeAll）已覆盖清理需求，删除 completed + error + cancelled 三类
4. 失败 Tab 有"重试全部"已够用

### 改动（4 个文件）

**QueueOperationController.js**：删除 cancelAll 方法（~65 行），更新文件头注释
**index.js**：
- 删除 cancelAll 代理（:334）
- destroy() 删除 this.cancelAll() 调用（destroy 只应清理资源，不应取消用户任务）
- 删除 cancelTokenSources 字段（从未被写入，死代码）
- 删除 cancelAllUploads/createCancelToken/cancelUpload 三个死代码方法（均 0 调用方）
**UploadLifecycle.js**：删除 cleanupAfterUpload 方法（0 调用方，引用已删除的 cancelTokenSources）
**fileUpload.js**：删除未使用的 message import（uploadFileToFolder 删除后遗留）

### 验证
- read_lints：0 error（3 个 pre-existing HINT）
- 全局搜索 cancelAll/cancelAllUploads/createCancelToken/cancelTokenSources/cleanupAfterUpload：0 真实引用（仅注释残留）
- jest：8 suite passed / 6 failed, 183 passed / 37 failed——与前五步完全一致

### 关键经验
- destroy() 不应取消用户任务，只应清理资源（定时器/监听器/Worker）
- cancelTokenSources 从未被写入却存在 4 个方法操作它——典型的"死代码滋生死代码"
- 删方法后要检查引用它的其他方法（cleanupAfterUpload 引用 cancelTokenSources），一并清理
- "批量取消需立即更新 UI"的注释理由是假的——代码注释里的"原因"可能是为不存在的功能写的
