# 2026-07-05 工作日志

## 资料库删除文件夹后物理目录残留修复

### 问题根因
`PhysicalFolderCleaner.delete()` 用 `settings.DOCUMENT_STORAGE_PATH`（默认 `/data/spug/documents`，但 settings 未定义该变量）作为 base，且路径格式 `folder_{id}`（下划线）。实际上传路径用 `os.path.join(settings.BASE_DIR, 'storage', 'documents')` 为 base，格式 `public/folder-{id}` 或 `private/user-{user_id}/folder-{id}`（短横线）。双重不匹配导致物理目录残留。

### 修改文件（3 个业务文件 + 1 个测试）
- `spug_api/apps/document/services/cleanup_service.py`（+170/-35）
  - 新增 `_get_storage_documents_base()`：统一获取 `storage/documents` 根目录
  - 新增 `cleanup_parent_dirs_safe(parent_dirs)`：基于真实 file_path 父目录的兜底清理（模块级，供所有删除流程复用）
  - 重写 `PhysicalFolderCleaner.delete(folder, is_public=None, user_id=None)`：优先复用 `get_document_absolute_path()` 规则（folder-{id} 短横线），保留旧格式 `folder_{id}` 兼容清理
  - `ContentDeleter` 增加 `is_public` 参数 + `delete_files_in_folder` 收集 file_path 父目录并兜底清理
  - `FolderCleanupService` 传入 `is_public`
- `spug_api/apps/document/tasks/cleanup/base.py`（+9）
  - `_delete_physical_folder_safe(folder, is_public=None, user_id=None)` 向后兼容签名
- `spug_api/apps/document/views/folder/views.py`（+22）
  - `FolderView._delete_folder` 第三步传 `is_public`/`user_id`
  - 第二步收集 file_path 父目录，第三步后调用 `cleanup_parent_dirs_safe`
- `spug_api/tests/test_physical_folder_cleanup.py`（新增，7 个场景 16 项断言）

### 安全设计
- 所有待删除路径必须通过 `is_safe_path(storage/documents, target)` 校验
- 跳过空路径、根存储目录（`storage/documents` 本身）、不存在的路径
- 不使用用户输入直接拼接删除路径
- 用 `seen` 集合去重，避免重复处理

### 兼容设计
- 保留旧格式 `folder_{id}`（下划线）清理能力，用于历史残留目录
- 不影响 `file.delete(hard=True)` 现有逻辑
- 不改变数据库模型结构、不改变上传路径生成规则
- `PhysicalFolderCleaner.delete` 新增参数有默认值（None），向后兼容旧调用

### 验证
- 本地 `python -m py_compile` 4 文件全部通过
- Docker `tdyw-test` 容器内 `py_compile` 3 业务文件通过
- 测试脚本 16/16 通过：
  1. 私有空间删除 `folder-{id}` 目录 ✓
  2. 公共空间删除 `folder-{id}` 目录 ✓
  3. file_path 父目录兜底清理 ✓
  4. 旧格式 `folder_{id}` 兼容清理 ✓
  5. 非 storage/documents 路径被拒绝 ✓
  6. 根存储目录被拒绝删除 ✓
  7. 不传 is_public 时从 `folder.TENANT_TYPE` 推断 ✓

### 环境备注
- Windows PowerShell 下 `wsl -d Ubuntu-22.04 -e bash -c "docker ..."` 调用容器
- PowerShell 会解析双引号内的 `:`，docker exec 内层命令必须用单引号包裹：`docker exec tdyw-test bash -c '...'`
- 测试输出重定向到容器内 `/tmp/test_out.txt` 再 `docker cp` 出来读取，避免 PowerShell 截断
- Django 测试脚本需 `sys.path.insert(0, '/data/spug/spug_api')` 才能导入 `spug.settings`

## 升级方案 - 系统下拉框统一为 SystemSelect

### 背景
"新建方案 - 系统"下拉框（PlanForm.js）用 antd Select + `store.filterOptions.systems`，会合并历史升级记录的系统名，可能含已停用项，且无搜索/新增/移除能力，与"新建升级申请 - 升级系统"（用 `../components/SystemSelect`）表现不一致。

### 修改文件（2 个，纯前端）
- `spug_web/src/pages/upgrade/plan/PlanForm.js`
  - 新增 `import SystemSelect from '../components/SystemSelect';`
  - system 字段 Select 替换为 `<SystemSelect placeholder="请选择或输入系统" />`
  - 保留升级类型/默认状态/步骤阶段的 Select 不变
- `spug_web/src/pages/upgrade/plan/PlanManager.js`
  - useEffect 增加 `store.fetchSystems();`，确保 SystemSelect 有 active 候选项

### 关键点
- SystemSelect 数据源是 `store.systems`（仅 active 字典项），停用后立即从下拉消失
- `store.filterOptions.systems`（含历史兜底）仅用于列表筛选，不用于表单
- 字段名仍为 `system`，提交值仍为系统名称字符串，后端数据结构未变
- 编辑回显：`form.setFieldsValue({ system: initialValues.system })` + SystemSelect 透传 value/onChange，正常工作
- 未改 SystemSelect 权限/新增/删除逻辑，未改 `/api/upgrade/systems/` 后端接口
- 升级表单参考用法：`CreateUpgradeModal.js:46` 和 `WorkbenchForm.js:629` 均直接 `<SystemSelect />`

### 验证
- read_lints 两个文件均通过（0 错误）
- 改动为 JSX 元素替换 + import 一行 + useEffect 一行，常规模式

## 升级系统候选项字典租户隔离修复

### 背景
`UpgradeSystem` 字典表原来 `tenant_id=''` 全局共享，所有租户共用同一套系统候选项。新增写死 `tenant_id=''`，删除/停用跨租户检查 `UpgradeRecord`，筛选选项也跨租户合并。需要改为按租户隔离。

### 修改文件（3 个：2 业务文件 + 1 迁移）
- `spug_api/apps/upgrade/views/systems.py`
  - GET: `filter(tenant_id=request.user.tenant_id, is_active=True)`
  - POST: `tenant_id=request.user.tenant_id`，查重 `filter(tenant_id=tenant_id, name__iexact=name)`，sort_order 限定同租户，create 用 `tenant_id=tenant_id`（不再写死 `''`）
  - DELETE: `get(pk=pk, tenant_id=tenant_id)`，历史记录检查 `filter(tenant_id=tenant_id, system=name)`
- `spug_api/apps/upgrade/services/record_service.py`
  - `get_filter_options()`: `UpgradeSystem.objects.filter(tenant_id=tenant_id, is_active=True)`
- `spug_api/apps/upgrade/migrations/0014_tenant_isolate_upgrade_systems.py`（新增）
  - 数据迁移：收集非空租户 ID（User + UpgradeRecord distinct），将 `tenant_id=''` active 项复制到每个非空租户
  - 保留 `tenant_id=''` 原始记录不停用（属于默认/管理员租户）
  - `get_or_create` 避免重复，`created_by` 不复制（避免 PROTECT）

### 关键设计决策
- **直接用 `request.user.tenant_id` 过滤，不用 `apply_tenant_filter`**：后者对超管/全局管理员不过滤，会导致跨租户泄漏
- **`unique_together = [['tenant_id', 'name']]` 已正确**：不同租户允许同名，同租户内不可重复
- **保留 `tenant_id=''` 全局数据**：属于默认/管理员租户（`make_tenant_id()` default=`''`），停用会导致管理员租户无候选项
- **迁移用复制而非清理**：保证现有非空租户不丢失预设系统，避免回归

### 验证
- py_compile 3 文件全部通过
- read_lints 3 文件均 0 错误
- git diff 确认改动范围正确
