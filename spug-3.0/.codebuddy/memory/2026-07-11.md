# 2026-07-11 工作日志

## 附件预览：图片/PDF 不走 kkFileView，改浏览器原生预览

**背景**：用户反馈图片/PDF 没必要走 kkFileView（多一层中转、慢、依赖外部服务），应直接浏览器内联预览。

**改动方案**（5 文件，+81/-20 行）：

### 后端
1. `apps/evidence/attachment_service.py` — `download_response` 新增 `inline=False` 参数
   - `inline=True` 时用 `mimetypes.guess_type` 推断 Content-Type，`Content-Disposition: inline`
   - `inline=False` 保持原逻辑（`application/octet-stream` + `attachment`）
2. 三个模块的 `AttachmentDownloadView` 透传 `inline` 参数：
   - `apps/upgrade/views/upload.py`
   - `apps/radio_license/views.py`
   - `apps/contract_agreement/views.py`
   - 统一写法：`inline = request.GET.get('inline') in ('1', 'true', 'True')`

### 前端
`spug_web/src/components/AttachmentManager.js`（公共组件，改一处全模块生效）：
- 新增 `IMAGE_EXTENSIONS` / `PDF_EXTENSIONS` 常量 + `isImageFile` / `isPdfFile` 辅助函数
- `handlePreview` 分流：
  - 图片/PDF → 拼下载 URL `?x-token=...&inline=1`，设 `previewType='image'/'pdf'`
  - 其他类型 → 仍调 `preview-url` 接口走 kkFileView
- `AttachmentPreviewModal` 新增 `previewType` prop：
  - `image` → `<img>` 渲染
  - `pdf` / `kkfileview` → `<iframe>` 渲染
- 预览按钮条件从 `previewUrlPrefix` 放宽为 `(previewUrlPrefix || downloadUrlPrefix)`

**设计原则**：
- 图片/PDF 浏览器原生支持，不需要 kkFileView 转换
- Office 文档（doc/xls/ppt 等）仍需 kkFileView 转换为 HTML/PDF
- inline 模式复用下载接口鉴权（x-token），无需额外预览令牌

## 合同协议模块对齐无线电台执照（设计决策，尚未执行）

**用户要求（4 点）**：
1. 删掉合同协议的"提醒状态"（remind_status 字段/列/计算）
2. 合同 `status` 字段设置改成和执照相同：`CharField(max_length=20, default='normal')`，无 choices，值 normal/expiring/expired（由扫描任务实时算）
3. 仿执照加"责任人"：模型加 `responsible_user_id`(IntegerField null) + `responsible_user_name`(CharField)，必填、远程搜索选人
4. 提醒做成和执照一样：弹窗 `_unacked_reminder_queryset` 加 `responsible_user_id=request.user.id` 过滤（只推给责任人），去掉 remind_status，改用 `remind_type` + `days_left`；badge 对齐 `RadioLicenseBadgeView`

**关键代码锚点**：
- 执照模型 `radio_license/models.py:20-22`（status 无 choices + 责任人两字段）
- 执照扫描 `radio_license/tasks.py:26-54`（`calculate_license_status` 返回 normal/expiring/expired）
- 执照责任人校验 `radio_license/views.py:32-44` + `ResponsibleUserListView:759-781`
- 执照弹窗 `radio_license/views.py:685-689`（按 responsible_user_id 过滤）
- 合同当前 `contract_agreement/models.py:26-44`（status 有 choices active/expired，无责任人）、`tasks.py:27-45`、`views.py:71-93`（`_serialize_agreement` 算 remind_status）、`:380-397`（_unacked_reminder_queryset）
- 合同迁移仅 `0001_initial.py`，需新增 `0002` 加字段 + 改 status + 数据迁移重算 status
- 前端：执照 `store.js:42-86`（responsibleUsers/fetchResponsibleUsers）、`Form.js:231-261`（责任人 Select）、`Table.js:114`（责任人列）；合同对应 `store.js`/`Form.js`/`Table.js` 需镜像并删 REMIND_TAG_MAP/提醒状态列

**注意**：迁移必须用 `makemigrations contract_agreement` 指定 app，避免扫全库；旧数据 status 为 active/expired，需数据迁移按 valid_end_date 重算为 normal/expiring/expired。tenant isolation 全程保持（`apply_tenant_filter` + responsible-users 视图非超管按 tenant_id 过滤）。

### 提示词输出（16:43）
已完成两个模块全量对比（models/tasks/views/urls/celery_beat + 前端 store/Form/Table + 全局提醒组件 ContractAgreementReminderNotification/ContractAgreementBadgeStore），向用户输出了一份完整改造提示词，涵盖：后端 5 文件（models/tasks/views/urls/migration）+ 前端 3 文件（store/Form/Table）+ 全局组件可选调整。核心改造点：status 两态→三态、删 remind_status、加责任人字段+下拉接口、ReminderPopupView 按责任人过滤、BadgeView 对齐执照。用户尚未确认是否执行。

### 代码审查 + 迁移执行（16:55）
用户用另一个 AI 改好了代码（7 文件改 + 1 新 migration），审查发现 2 个问题并修复：

1. **Table.js 第41行 bug（必修）**：`STATUS_TAG_MAP.active` → `STATUS_TAG_MAP.normal`
   - STATUS_TAG_MAP 已改为三态（normal/expiring/expired），不再有 active 键
   - 命中 fallback 时 `STATUS_TAG_MAP.active` 为 undefined，`<Tag color={undefined.color}>` 会崩溃
2. **views.py 责任人必填不一致**：JsonParser 中 `responsible_user_id` 原为 `required=False`（选填），与执照（必填）和前端（required: true）不一致；`_validate_and_fill_responsible_user` 中 `if not form.responsible_user_id: return None` 让空值跳过校验。已改为必填（去掉 required=False + 去掉空值跳过）。

验证结果：
- py_compile：5 个后端文件全部通过
- 前端 lint：0 errors
- makemigrations --check：无新迁移（模型与数据库一致）
- migrate：0002 迁移成功执行
- 数据验证：1 条旧记录 status 从 active 正确重算为 expiring，ack 表 1 条记录保留（RenameField 成功），责任人字段 NULL（旧数据合理）

**WSL Docker 操作技巧**：Windows 下 Docker Desktop 在 WSL 中，需用 `wsl docker exec ...` 调用；PowerShell 的引号嵌套（bash -c 内含 Python -c 内含 SQL 引号）极易出错，解决方案是把脚本写到文件 → `docker cp` 到容器 → `docker exec` 执行 → 删除临时文件。执行 Django 脚本需要 `DJANGO_SETTINGS_MODULE=spug.settings` + `PYTHONPATH=/data/spug/spug_api`。

## AttachmentManager.js 函数行数超标修复（16:47）

**问题**：`AttachmentManager` 组件函数 ~203 行，超过 200 行限制。

**修复**：将预览相关 4 个 state（previewVisible/previewUrl/previewFileName/previewType）+ 2 个 handler（handlePreview/closePreview）抽取为自定义 hook `useAttachmentPreview(module, downloadUrlPrefix, previewUrlPrefix)`，组件内解构使用。函数从 ~203 行降至 171 行。

**文件**：`spug_web/src/components/AttachmentManager.js`

## 合同协议迁移状态清理 + MEMORY 精简（后续补充）

**背景**：合同协议对齐执照的代码（7 文件 + 0002 迁移）此前已由另一 AI 实现并审查通过。今日复查发现 `makemigrations --check --dry-run contract_agreement` 仍会提议 `0003_alter_contractagreement_managers_and_more`，原因是 0001（Codex 生成）的迁移态与模型不一致：
- 5 个 `Meta.indexes` 在 0001 中内联于 CreateModel 且用了非标准索引名（与模型自动生成名不符）→ 提议 RenameIndex
- 0001 显式记录了 `managers=[('objects', TenantModelManager())]`，而 radio_license 的干净 0001 是 manager-less → 提议 Change managers
- 其中 ack_valid_to 字段 help_text 与 0001 不一致 → 额外 AlterField

**修复（对齐 radio_license 的干净状态）**：
1. `models.py`：为 5 个 `Meta.indexes` 显式加 `name=`，值与 0001 已建索引名一致（消除 RenameIndex）
2. `0001_initial.py`：删除两处 `managers=[...]` 块，改 manager-less（与 radio_license 一致，消除 Change managers）
3. `0002_three_state_and_responsible_user.py`：新增 `AlterField ack_valid_to` 使 help_text 与模型一致（消除多余的 AlterField 提议）

**验证**：`makemigrations --check --dry-run contract_agreement` → "No changes detected"；`manage.py check contract_agreement` → "no issues"；py_compile 全过；前端 lint 0 errors。

> 注：上述 0001 清理属于修正 Codex 生成产物的历史不一致，非业务变更；DB 索引名/管理器不受影响。本任务严格遵守"只动 contract_agreement，不碰 radio_license"。

**MEMORY.md 精简**：原 MEMORY.md 超限（402 行）已重写精简，保留附件架构、Django 升级路线、迁移纪律、验证流程、antd 版本、角色委派、导出架构、反思清单等关键长期记忆。

## 主菜单改版：无线电台执照 → 执照管理（二级 + 台站频率批复占位）

**用户决策**：两个菜单都是执照类业务，一级菜单命名为「执照管理」（非「无线电台站」）。

**改动**（前端，2 文件）：
1. `spug_web/src/routes.js`：原顶级菜单「无线电台执照」降级为「执照管理」一级下的二级；新增二级「台站频率批复」→ `/station-frequency-approval`（占位页）。「无线电台执照」原功能 `RadioLicenseIndex` 未动。
2. `spug_web/src/pages/stationFrequencyApproval/index.js`：新建占位页，显示「功能开发中」（antd Empty）。后续真正开发时直接替换该目录内容。

**权限**：一级「执照管理」与「台站频率批复」暂复用 `radio_license.license.view`，保证可访问；后续台站频率批复独立开发时换专属权限码 + 同步 `codes.js`。

**验证**：read_lints 0 errors；@babel/parser（jsx）语法校验通过。

## 公告发布功能：编写实施提示词

**背景**：项目根目录已有 `公告发布功能设计方案.md`（892 行，14 章完整设计），用户要求据此编写一份可直接交给 AI 执行的实施提示词。

**方案要点**：
- 后端在 `apps/home` 下新增 `Announcement`/`AnnouncementScope`/`AnnouncementRead` 三模型 + `announcement.py` 接口，保留旧 `Notice` 兼容。
- 附件复用 `AttachmentService`（module=announcement），不新建附件表。
- 发布范围首期用 `Tenant` 作为"部门"。
- 三态：unpublished/published/expired，接口实时返回 computed_status。
- 管理端 `/api/home/announcement/admin/`，用户端 `/api/home/announcement/`。
- 前端：首页 AnnouncementPanel + 详情 Drawer + 管理端 system/announcement 页面 + routes.js 菜单 + codes.js 权限码。

**提示词覆盖**：技术栈硬约束（Docker/MariaDB/antd4.21.5/legacy decorators）、迁移纪律（指定 app + 手动命名）、后端落地清单、前端落地清单、状态业务规则、分阶段实施、验证流程、禁止事项。用户尚未确认是否执行实施。
