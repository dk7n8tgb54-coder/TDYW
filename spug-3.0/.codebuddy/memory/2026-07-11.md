# 2026-07-11

## 公告发布功能(Announcement)收尾决策
- 权限码配置走 `pages/system/role/codes.js` + **角色管理-功能权限 UI**(超级管理员勾选保存写回 `page_perms`),**不要**为角色写 `*.sql` 预置。误建的 `spug_api/apps/home/permissions.sql` 已删除(document/radio_license 的 permissions.sql 只是后期手动 convenience 脚本,非范本)。
- 菜单位置:用户端查询页 `/announcement`(从首页面板"更多"进入,不在菜单);后台管理页原挂在 `系统管理` 下,经用户确认改为**独立顶层菜单"公告管理"**(`routes.js`,`NotificationOutlined` 图标,auth `home.announcement.view`)。理由:系统管理=系统配置(账户/角色/设置/租户/日志),公告是面向用户发布的内容,语义不该归入系统配置。
- 权限分组(codes.js 的 `home`/"首页与公告")与菜单位置解耦,菜单怎么放都不影响权限分配。

## 安全题库模块实现提示词编写
- 根据 `安全题库模块设计方案.md` 编写了 `安全题库实现提示词.md`,供另一个 AI 实现该模块。
- 提示词包含:项目技术栈约束、迁移纪律、分阶段任务(4 阶段)、关键实现约束、参考文件清单。
- **重要架构变更**:用户确认"一个租户 = 一个科室",因此方案中的 Department 表和所有 department_id/department_name 字段全部去掉,改用 tenant_id。SafetyExamTarget 的 department_id 改为 target_tenant_id。全平台视图靠超管/全局管理员自动绕过 apply_tenant_filter 实现。
- 关键字段映射表已写入提示词,优先级高于方案原文。

## announcement.py 复杂度修复
- `AnnouncementAdminListView.post` 复杂度 22（限制 15）：校验+创建+更新全塞一个方法
- 提取 3 个模块级辅助函数：`_validate_announcement_form`（标题/内容/时间/范围校验）、`_update_announcement`、`_create_announcement`
- post 方法降至复杂度 ~4，逻辑清晰：权限→解析→校验→创建或更新→返回
