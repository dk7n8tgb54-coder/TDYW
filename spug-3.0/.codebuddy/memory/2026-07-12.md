# 2026-07-12 工作日志

## 台站频率批复（StationFrequencyApproval）设计方案
- 用户要在「执照管理」下新增「台站频率批复」子模块，字段：批复名称、涉及频率、起始/截止时间、对应执照、剩余天数状态、责任人、创建时间、操作。
- 核心诉求：执照与批复**双向关联、点击互跳**。
- 已出设计方案 MD：`站台频率批复设计方案.md`，**尚未实施**（用户偏好先认可方案再落地）。
- 设计要点：复用 radio_license 现有模式；新增 2 表（主表 + 频率明细）；状态实时计算不落库；跳转用路由 `?focus=<id>` + 详情 Modal 自动打开，零新增页面。
- routes.js 已有占位 `StationFrequencyApproval`（路径 `/station-frequency-approval`）。
- 待确认：执照是否必填、频率建模（明细表 vs 文本）、是否首版做导出/红点。
