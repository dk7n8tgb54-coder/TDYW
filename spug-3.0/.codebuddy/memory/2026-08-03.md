# 2026-08-03 工作日志

## 周报提醒功能（home 模块新增）

**需求**：解决科室人员经常忘记写周报、科长每周手动催办的问题。用户确认方案一（轻量级提醒，放在 home 模块下），要求提醒频率/时间点可配置、接收人可手动选择。周报在系统外完成，系统只做提醒。

**设计决策**：
- 放在 `home/` 模块下（与公告/通知同级，都是首页级消息功能）
- 不直接复用 ExpirationReminderNotification（语义不同：到期提醒是一次性截止日期，周报提醒是周期性催办）
- 不合并进公告管理（公告是信息广播，周报提醒是催办任务，语义混淆）
- 菜单分组合并：routes.js 中"公告管理"+ "周报提醒"合并为"公告与提醒"父级菜单
- 权限码挂在 home 组下：`home.weekly_report_reminder.view/add/edit/delete`

**后端实现（4 修改 + 2 新增）**：
- `home/models.py`：新增 `WeeklyReportReminder`（提醒规则）+ `WeeklyReportReminderLog`（发送/确认记录）
  - recipient_users 存 JSON 字符串（`[{"id":1,"nickname":"张三"}]`）
  - remind_days 存逗号分隔（"1,4" = 周一、周四，1-7 约定）
  - 唯一约束 (reminder_id, user_id, week_key) 防重复发送
  - TenantModelMixin 软删除模式
- `home/weekly_report.py`（新增）：CRUD + pending + ack + users + status 看板
  - ⚠️ 用 `request.body` 解析（不是 `request.POST`），与项目约定一致
  - `patch` 方法委托给 `post`（pk 区分新增/编辑）
- `home/tasks.py`：新增 `check_weekly_report_reminders`，每 5 分钟轮询
  - 时间窗口 10 分钟（配合 5 分钟轮询，允许一次延迟）
  - `get_or_create` + 唯一约束 = 幂等
- `home/celery_beat_schedule.py`：新增 beat 条目 `weekly-report-reminder-check`
- `home/urls.py`：注册 6 条路由
- `0010_weekly_report_reminder.py`（迁移文件已生成，DB 损坏待恢复后执行 migrate）

**前端实现（3 新增 + 3 修改）**：
- `components/WeeklyReportReminderNotification.js`（新增）：
  - 每 5 分钟轮询 pending 接口
  - `notification.info` 弹窗 + "我去写了"(ACK) + "稍后提醒"(localStorage 静默 1 小时)
  - 用户 ID 维度的 localStorage 去重
- `pages/system/weekly-report-reminder/index.js`（新增）：
  - 管理端 CRUD + 启用/停用 Switch + "本周确认状态"看板（展开行显示每人确认状态 + 进度条）
  - 接收人选择器：多选 Select，从 `/users/` 接口加载
  - 提醒日：Checkbox.Group（周一~周日）
  - 提醒时间：TimePicker（HH:mm, 5 分钟步进）
- `layout/index.js`：挂载 `<WeeklyReportReminderNotification/>`
- `routes.js`：公告管理 + 周报提醒合并为"公告与提醒"父级菜单
- `codes.js`：新增 `weekly_report_reminder` 权限页（view/add/edit/delete）

**环境问题**：
- `tdyw-test` 容器切换成功（bind mount 确认）
- `tdyw-db-test` InnoDB 表空间损坏（`Data structure corruption`），无法执行 migrate
- 迁移文件 `0010_weekly_report_reminder.py` 已生成且 py_compile 通过
- 待 DB 恢复后执行：`docker exec tdyw-test python manage.py migrate home`

**DB 修复后验证**：
- migrate 已成功执行，两张表 `tdyw_weekly_report_reminders` + `_logs` 创建正确
- Django check 通过，API 路由返回 401（需登录）正常

**前端 bug 修复（第二轮）**：
1. **TimePicker 需要 moment 对象**：`openCreate`/`openEdit` 之前传字符串，改为 `moment('17:00', 'HH:mm')`
2. **后端 TimeField 序列化带秒**：`to_dict()` 返回 `"17:00:00"`，后端 parser 只认 `"%H:%M"` -> 报"提醒时间格式错误"
   - 修复1：`to_view()` 中 `remind_time.strftime('%H:%M')` 统一输出不带秒
   - 修复2：parser 兼容 `"%H:%M"` 和 `"%H:%M:%S"` 两种格式
3. **`handleSubmit` 时间转换不可靠**：用了 `_d` 内部属性，改为 `values.remind_time.format('HH:mm')`
4. **`toggleEnabled` 散播整个 record**：改为显式传必要字段，`remind_time` 截断为 5 字符
5. **表格 remind_time 显示带秒**：`render` 加 `t.substring(0, 5)` 兜底

## 密码中文字符漏洞修复

**问题**：账号创建/修改密码时可输入中文字符。根因：`verify_password` 用 `[^a-zA-Z0-9]` 匹配"特殊字符"，中文不属于 `[a-zA-Z0-9]`，被误判为合法特殊字符通过校验。

**修复**：
- `apps/account/utils.py`：新增 `re.fullmatch(r'[!-~]+', password)` 校验，仅允许可打印 ASCII（0x21-0x7E），拒绝中文/emoji/空格/控制字符；加 `isinstance(password, str)` 防 None
- `apps/account/views.py`：3 处错误提示文案统一更新为"仅限英文、数字、符号（不可含中文）"
- 前端 3 处 `extra` 提示同步更新（Reset.js / Form.js / Table.js）

**验证**：容器内单元测试 7/7 PASS（中文密码/emoji/空格/短密码/None 均被拒绝，正常 ASCII 密码通过）；py_compile + lint 通过

## 第三轮重构：字段重新设计 + 权限/租户隔离 + 首页面板

**字段重构**：
- 删除：`remind_days`、`remind_time`、`title`
- 新增：`target_date`（DateField）、`repeat_type`（none/daily/weekly/monthly/yearly）、`repeat_interval`（int）
- Log 表：`week_key` -> `date_key`，唯一约束改为 `(reminder_id, user_id, date_key)` 按天去重
- 模型新增 `matches_today(today)` 方法：根据 target_date + repeat 判断今天是否命中

**权限+租户隔离**：
- `_is_admin` -> `_can_manage(user, perm)`（检查 is_supper 或 has_perms）
- 列表/详情/编辑/删除全部加 `tenant_id=request.user.tenant_id` 过滤

## 数据分析模块方案

**产出**：`DATA_ANALYSIS_PLAN.md`

**现状**：前端已有占位路由 `/data-analysis`（无 auth），后端无 app。

**数据源**（用户确认）：设备履历 + 系统升级 + 故障管理 + 干扰管理（去掉执照/合同/跨日事项）。

**架构决策**：
- 后端纯只读聚合，不建 model/migration，跨 app 查询
- 复用 `bizcharts`（已有依赖）
- 5 个子权限码：overview / fault / interference / device / upgrade
- Redis 缓存聚合结果 60s
- 参考 `InterferenceStatisticsView` 模式：`apply_tenant_filter` + 日期范围参数

**分期**：P1 总览看板 -> P2 故障 -> P3 干扰 -> P4 设备 -> P5 升级

**Celery 任务简化**：
- 删除时间窗口判断（前后10分钟），改为 `matches_today` 判断
- 当天内任何时间 Celery 跑到都能补发

**首页面板**：
- 新增 `WeeklyReportReminderPanel.js`：Card 展示待确认提醒列表 + "我去写了"按钮
- 挂载在 `pages/home/index.js`，公告面板下方

## 铃铛永远加载 + 告警列表条数少 修复

**用户澄清**：两个问题都是关于铃铛/系统告警，不是操作日志。
**注意**：第一轮编辑未实际写入文件，第二轮重新编辑才生效，已通过 read_file 验证。

**问题 1 - 铃铛一直在加载中 + 有时候刷新完不显示（P0）**：
- 根因：`layout/Header.js` 是普通函数组件，直接读取了 `AlertStore` 的 MobX observable（`loading`/`recentAlerts`/`unreadCount`），但**没有用 `observer` 包裹**
- 触发流程：用户点铃铛 -> `setBellOpen(true)` -> 组件重渲染 -> `loading=true`（刚被 fetchRecent 设置）-> Spin 显示 -> API 完成 `loading=false` -> 但无 observer 不触发重渲染 -> Spin 永远不停
- "有时候刷新完不显示"：同理，fetchRecent 完成后 loading=false 但组件不重渲染
- 修复：`import { observer } from 'mobx-react'` + `export default observer(function (props) { ... })`

**问题 2 - 铃铛只显示七八条（轮询竞态）**：
- `AlertStore.fetch()`（30 秒轮询）用 `page_size=5`
- `AlertStore.fetchRecent()`（点开铃铛）用 `page_size=20`
- `fetchRecent` 拿回 20 条后，30 秒后 `fetch` 只拿 5 条覆盖掉，列表条数缩水
- 修复：
  1. `fetch()` 的 `page_size` 从 5 改为 20（统一常量 `FETCH_PAGE_SIZE`）
  2. 加 `if (!this.loading)` 守卫：铃铛加载中时轮询不覆盖 `recentAlerts`，但仍更新 `unreadCount`/`errorCount`

**告警数据量说明**：DB 里只有 7-8 条告警是正常数据量（系统健康则少告警）。告警由 Celery 生成（磁盘 10 分钟/次、DB 5 分钟/次），有冷却去重（5-30 分钟同 `alert_key` 不重复）。告警页面 (`/maintenance/alert`) 本身有 observer + pageSize=20，无默认过滤，显示的就是 DB 里的实际数量。

**顺手改进（audit/index.js）**：筛选切换时未重置页码 + 90 天默认过滤提示 Tag。

## 告警页面刷新后空表修复（同一根因第三次）

**问题**：系统告警页面 (`/maintenance/alert`) 刷新后空表，切换菜单再切回来才显示。

**根因**：`pages/system/alert/index.js` 中 `AlertTable` 是普通函数组件，读取了 `store.records`/`store.isFetching`/`store.summary`/`store.total` 等 MobX observable，但**没有 `observer` 包裹**。父组件 `AlertIndex` 有 `observer` 但不直接读这些字段，所以 `fetchRecords` 完成后不触发重渲染。

**修复**：`AlertTable` 加 `observer` 包裹。

**教训（重要模式）**：直接读取 MobX observable 的组件**必须**用 `observer` 包裹，否则数据更新后不触发重渲染。本轮三次修复都是同一根因：
1. `Header.js` - 铃铛永远加载
2. `AlertStore.js` - 轮询竞态
3. `alert/index.js` - 告警页面刷新空表

## 提醒事项代码质量修复（复杂度/行数超标）

**问题**：
- 后端 `apps/reminder/views.py:59` `post` 方法复杂度 18（限制 15）：校验+更新+创建全塞一个方法
- 前端 `pages/reminder/index.js:41` `ReminderAdmin` 组件约 300 行（限制 200）：表单弹窗+状态弹窗+列表全在一个组件

**后端重构**（复杂度 18 -> ~6）：
- 提取 `_validate_reminder_form(form)` 模块级函数：日期+重复类型+接收人校验，返回 `((target_date, recipients), None)` 或 `(None, error)`
- 拆分 `post` 为 `post` + `_save_update` + `_save_create` 三个方法
- `post` 现在只做：权限检查 -> 参数解析 -> 校验 -> 分派

**前端重构**（300 行 -> ~130 行主组件）：
- 新增 `ReminderFormModal.js`（~115 行）：表单弹窗，含 form state + handleSubmit
- 新增 `ReminderStatusModal.js`（~65 行）：今日确认状态弹窗
- `index.js` 精简为 ~130 行：列表 + 操作 + 子组件引用
- 主组件通过 props 传递 `visible`/`editing`/`users`/`onCancel`/`onSuccess` 给子组件

**验证**：lint 0 errors（前后端）+ py_compile exit 0
