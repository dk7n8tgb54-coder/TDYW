# 资料库断点续传优化方案

## 一、现状诊断

### 1.1 发现的严重问题

| 问题编号 | 问题描述 | 严重程度 | 影响范围 |
|---------|---------|---------|---------|
| P0-1 | 前端恢复传输记录后**未检查已上传分片** | 🔴 严重 | 大文件断点续传完全失效 |
| P0-2 | 恢复后`file`对象为`null`，无法继续上传 | 🔴 严重 | 用户必须重新选择文件 |
| P0-3 | `resumeItem`方法缺少分片检查逻辑 | 🟠 高 | 恢复上传时从头开始 |
| P1-1 | 缺少分片校验机制 | 🟡 中 | 可能上传重复分片 |
| P1-2 | 恢复时没有更新后端`uploaded_chunks`计数 | 🟡 中 | 进度显示不准确 |

### 1.2 代码位置

```
问题文件: spug_web/src/pages/document/stores/upload/core/index.js
问题方法: resumeItem() (第519行)
```

**当前实现缺陷：**
```javascript
// 当前代码只检查后端状态，没有检查已上传分片
resumeItem(itemId) {
  // ...检查后端状态...
  
  // 恢复上传
  if (item.file && item.file instanceof File) {
    item.status = 'uploading';
    // ❌ 缺少: 检查已上传分片
    // ❌ 缺少: 从上次断点继续
    await this.uploadSingleFile(item.file, ...); // 会从头开始上传
  }
}
```

---

## 二、问题详细分析

### 2.1 后端已实现的断点续传接口

后端已提供完整的断点续传支持：

| 接口 | 端点 | 功能 |
|-----|------|------|
| 检查已上传分片 | `POST /api/document/check_uploaded_chunks/` | 返回已上传的分片索引列表 |
| 分片上传 | `POST /api/document/upload_chunk/` | 上传单个分片 |
| 合并分片 | `POST /api/document/merge_chunks/` | 合并所有分片 |

**后端接口响应示例：**
```json
{
  "exists": true,
  "uploaded_chunks": [0, 1, 2, 5, 6],  // 已上传的分片索引
  "count": 5,
  "message": "找到 5 个已上传分片",
  "merge_status": null,
  "merge_task_id": null
}
```

### 2.2 前端恢复流程缺陷

```
当前流程（有缺陷）：
1. 页面加载 → 调用 restoreTransfers()
2. 获取后端传输列表
3. 恢复到前端队列（file: null, chunks: []）
4. 用户点击继续 → 调用 resumeItem()
5. ❌ 直接开始上传，没有检查已上传分片
6. ❌ 从头开始上传所有分片

理想流程：
1. 页面加载 → 调用 restoreTransfers()
2. 获取后端传输列表
3. 恢复到前端队列
4. 用户点击继续 → 调用 resumeItem()
5. ✅ 调用 checkUploadedChunks() 检查已上传分片
6. ✅ 跳过已上传分片，从断点继续
7. ✅ 只上传剩余分片
```

### 2.3 影响场景

| 场景 | 当前行为 | 预期行为 |
|-----|---------|---------|
| 上传500MB文件到50%时刷新页面 | 恢复后需重新上传全部500MB | 恢复后只需上传剩余250MB |
| 网络中断后恢复上传 | 从头开始上传 | 从断点继续上传 |
| 浏览器崩溃后恢复 | 无法继续，需重新选择文件 | 自动恢复，继续上传 |

---

## 三、优化方案

### 3.1 方案概述

```
┌─────────────────────────────────────────────────────────────┐
│                    断点续传优化架构                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐      ┌──────────────┐      ┌───────────┐ │
│  │  1. 恢复阶段  │ ───→ │  2. 检查阶段  │ ───→ │ 3. 上传阶段│ │
│  └──────────────┘      └──────────────┘      └───────────┘ │
│         │                     │                    │       │
│         ▼                     ▼                    ▼       │
│  ┌──────────────┐      ┌──────────────┐      ┌───────────┐ │
│  │ restoreTransfers│   │ checkUploaded │      │ uploadChunk│ │
│  │ 获取传输记录  │      │ 检查已上传分片 │      │ 从断点继续 │ │
│  └──────────────┘      └──────────────┘      └───────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 详细实现步骤

#### 步骤1: 新增分片检查API调用方法

**文件**: `spug_web/src/pages/document/stores/upload/core/transfer.js`

```javascript
/**
 * 检查已上传分片（断点续传）
 * @param {string} fileHash - 文件MD5哈希
 * @param {number} fileSize - 文件大小
 * @param {number} totalChunks - 总分片数
 * @param {boolean} isPublic - 是否公共空间
 * @returns {Promise<{exists: boolean, uploaded_chunks: number[], count: number}>}
 */
async checkUploadedChunks(fileHash, fileSize, totalChunks, isPublic = false) {
  try {
    const { http } = await import('libs');
    const result = await http.post(API_ENDPOINTS.CHECK_UPLOADED_CHUNKS, {
      file_hash: fileHash,
      file_size: fileSize,
      total_chunks: totalChunks,
      is_public: isPublic
    });
    return result;
  } catch (error) {
    console.error('[TransferStore] 检查已上传分片失败:', error);
    // 失败时返回空列表，视为从头开始
    return { exists: false, uploaded_chunks: [], count: 0 };
  }
}
```

**需要添加的API端点** (`constants/api.js`):
```javascript
export const API_ENDPOINTS = {
  // ... 其他端点
  CHECK_UPLOADED_CHUNKS: `${BASE_URL}/check_uploaded_chunks/`,
}
```

#### 步骤2: 修改 resumeItem 方法支持断点续传

**文件**: `spug_web/src/pages/document/stores/upload/core/index.js`

```javascript
@action
async resumeItem(itemId) {
  const item = this.queueStore.findUploadItemInCurrentTenant(itemId);
  if (!item) return;

  if (!['paused', 'error', 'waiting'].includes(item.status)) {
    return;
  }

  // 检查后端状态
  if (item.transferId) {
    try {
      const transfers = await this.transferStore.fetchTransfers(
        this.rootStore.navigationStore?.isPublic
      );
      const currentTransfer = transfers.find(t => t.id === item.transferId);
      
      if (currentTransfer) {
        const backendStatus = currentTransfer.status;
        
        if (backendStatus === 'COMPLETED') {
          item.status = 'completed';
          item.percent = 100;
          item.error = null;
          return;
        }
        
        if (backendStatus === 'CANCELED' || backendStatus === 'FAILED') {
          item.status = 'error';
          item.error = backendStatus === 'CANCELED' ? '已取消' : '失败';
          return;
        }
        
        // 【新增】同步后端的 file_hash 和 uploaded_chunks
        if (currentTransfer.file_hash && !item.fileHash) {
          item.fileHash = currentTransfer.file_hash;
        }
        if (currentTransfer.uploaded_chunks) {
          item.currentChunk = currentTransfer.uploaded_chunks;
        }
      }
    } catch (error) {
      console.error('[resumeItem] 获取传输状态失败:', error);
    }
  }

  // 【核心修复】检查是否有文件对象
  if (!item.file || !(item.file instanceof File)) {
    // 大文件断点续传需要重新选择文件
    if (item.fileSize > UPLOAD_CONSTANTS.NORMAL_UPLOAD_THRESHOLD) {
      message.warning(`"${item.name}" 需要重新选择文件才能继续上传`);
      item.error = '请重新选择文件';
      item.status = 'error';
      
      // 【新增】触发文件选择对话框
      this.triggerFileReSelection(itemId);
      return;
    } else {
      // 小文件直接标记为错误
      item.status = 'error';
      item.error = '文件对象丢失，请重新上传';
      return;
    }
  }

  // 【核心修复】大文件断点续传检查
  if (item.fileSize > UPLOAD_CONSTANTS.NORMAL_UPLOAD_THRESHOLD && item.fileHash) {
    try {
      message.loading('正在检查已上传分片...', 0);
      
      const chunkCount = Math.ceil(item.fileSize / UPLOAD_CONSTANTS.CHUNK_SIZE);
      const checkResult = await this.transferStore.checkUploadedChunks(
        item.fileHash,
        item.fileSize,
        chunkCount,
        item.isPublic
      );
      
      message.destroy();
      
      if (checkResult.exists && checkResult.uploaded_chunks.length > 0) {
        // 计算已上传字节数
        const uploadedCount = checkResult.uploaded_chunks.length;
        const uploadedBytes = uploadedCount * UPLOAD_CONSTANTS.CHUNK_SIZE;
        const percent = Math.min(Math.round((uploadedBytes / item.fileSize) * 100), 99);
        
        item.currentChunk = uploadedCount;
        item.percent = percent;
        item.chunks = checkResult.uploaded_chunks.map(idx => ({ index: idx, uploaded: true }));
        
        message.success(`已找到 ${uploadedCount} 个已上传分片，将从 ${percent}% 处继续`);
        
        // 【新增】更新后端 uploaded_chunks 计数
        if (item.transferId) {
          await this.transferStore.updateTransferProgress(item.transferId, percent);
        }
      }
    } catch (error) {
      message.destroy();
      console.error('[resumeItem] 检查已上传分片失败:', error);
      // 继续上传，从头开始
    }
  }

  // 恢复上传
  item.status = 'uploading';
  item.error = null;
  item.canAbort = true;
  item.isPausedByUser = false;
  item.isCancelledByUser = false;
  item.abortController = new AbortController();

  const resumeFolderId = item.folderId !== null ? item.folderId : this.rootStore.navigationStore?.currentFolderId;
  
  // 【修复】使用改进的 uploadSingleFile 方法，支持从指定分片开始
  await this.resumeChunkedUpload(item.file, resumeFolderId, item);
}
```

#### 步骤3: 新增断点续传专用上传方法

**文件**: `spug_web/src/pages/document/stores/upload/core/index.js`

```javascript
/**
 * 断点续传专用方法
 * 支持从指定分片继续上传
 */
@action
async resumeChunkedUpload(file, folderId, item) {
  const uploadId = item.id;
  const startChunkIndex = item.currentChunk || 0;
  
  try {
    // 如果还没计算MD5，需要重新计算
    if (!item.fileHash) {
      this.queueStore.updateUploadItem(uploadId, {
        status: 'calculating',
        error: '正在计算文件哈希...',
      });
      
      const fileHash = await this.rootStore.md5Store?.calculateFileMD5(file, uploadId);
      item.fileHash = fileHash;
      
      // 保存到后端
      if (item.transferId) {
        await this.transferStore.updateTransferHash(item.transferId, fileHash);
      }
    }

    const fileSize = file.size;
    const chunkCount = Math.ceil(fileSize / UPLOAD_CONSTANTS.CHUNK_SIZE);

    this.queueStore.updateUploadItem(uploadId, {
      status: 'uploading',
      canAbort: true,
      chunkCount: chunkCount,
      fileSize: fileSize,
    });

    // 创建AbortController
    const taskAbortController = new AbortController();
    this.queueStore.updateUploadItem(uploadId, {
      abortController: taskAbortController,
      isPausedByUser: false,
      isCancelledByUser: false,
    });

    // 【核心】从断点继续上传
    for (let chunkIndex = startChunkIndex; chunkIndex < chunkCount; chunkIndex++) {
      // 重新获取uploadItem
      const currentItem = this.queueStore.findUploadItemInCurrentTenant(uploadId);
      if (!currentItem) break;

      // 检查暂停/取消状态
      if (currentItem.isPausedByUser || currentItem.status === 'paused') {
        this.queueStore.updateUploadItem(uploadId, {
          status: 'paused',
          error: '已暂停',
          currentChunk: chunkIndex,
          canAbort: false,
        });
        break;
      }

      if (this.rootStore.isCancelled || currentItem.isCancelledByUser) {
        this.queueStore.updateUploadItem(uploadId, {
          status: 'error',
          error: '已取消',
          canAbort: false,
        });
        throw new Error('用户取消');
      }

      // 上传单个分片
      await this.chunkUploadStore.uploadSingleChunk(
        file, uploadId, chunkIndex, chunkCount, 
        item.fileHash, folderId, taskAbortController, item.isPublic
      );
      
      // 【新增】更新已上传分片计数
      item.currentChunk = chunkIndex + 1;
    }

    // 检查是否暂停
    const finalItem = this.queueStore.findUploadItemInCurrentTenant(uploadId);
    if (finalItem && finalItem.status !== 'paused') {
      // 合并分片
      await this.chunkUploadStore.mergeChunks(
        file, uploadId, chunkCount, item.fileHash, folderId, item.isPublic
      );
    }

  } catch (error) {
    const errorMsg = `（${file.name}）上传失败`;
    
    if (item.status !== 'paused') {
      item.status = 'error';
      item.error = error.message?.includes('已取消') ? '已取消' : errorMsg;
      item.canAbort = false;
      item.abortController = null;

      if (item.transferId && this.rootStore.transferStore) {
        await this.rootStore.transferStore.markTransferAsFailed(item.transferId, error.message || '上传失败');
      }
    }

    throw error;
  }
}
```

#### 步骤4: 新增文件重新选择功能

**文件**: `spug_web/src/pages/document/UploadPanel.js`

```javascript
// 在 UploadItem 组件中添加重新选择文件功能
const UploadItem = observer(({ item, onPause, onResume, onCancel, onRemove, onReSelectFile }) => {
  // ... 其他代码

  // 处理重新选择文件
  const handleReSelectFile = React.useCallback(() => {
    // 触发文件选择
    const input = document.createElement('input');
    input.type = 'file';
    input.onchange = (e) => {
      const file = e.target.files[0];
      if (!file) return;
      
      // 验证文件是否匹配
      if (file.name !== item.name) {
        message.error('文件名不匹配，请选择相同的文件');
        return;
      }
      if (file.size !== item.fileSize) {
        message.error('文件大小不匹配，请选择相同的文件');
        return;
      }
      
      onReSelectFile(item.id, file);
    };
    input.click();
  }, [item.id, item.name, item.fileSize, onReSelectFile]);

  // 在渲染中添加重新选择按钮
  // 当 file 为 null 且需要重新选择时显示
  if (!item.file && item.status === 'error' && item.error === '请重新选择文件') {
    return (
      <div className={styles.uploadItem}>
        {/* ... 其他UI ... */}
        <Button onClick={handleReSelectFile}>选择文件</Button>
      </div>
    );
  }
});
```

**在 UploadPanel 主组件中添加处理方法:**

```javascript
// 处理重新选择文件后继续上传
handleReSelectFile = async (itemId, newFile) => {
  const item = uploadCoreStore.queueStore.findUploadItem(itemId);
  if (!item) return;
  
  // 更新文件对象
  item.file = newFile;
  item.status = 'waiting';
  item.error = null;
  
  // 自动开始上传
  await uploadCoreStore.resumeItem(itemId);
}
```

#### 步骤5: 修改 chunkUpload.js 支持从指定分片开始

**文件**: `spug_web/src/pages/document/stores/upload/core/chunkUpload.js`

```javascript
/**
 * 【新增】支持从指定分片开始上传
 * @param {number} startChunkIndex - 起始分片索引（断点续传用）
 */
async uploadFileChunkedFromChunk(file, folderId, uploadId, isPublic = null, startChunkIndex = 0) {
  // ... 原有逻辑 ...
  
  // 从指定分片开始
  for (let chunkIndex = startChunkIndex; chunkIndex < chunkCount; chunkIndex++) {
    // ... 原有逻辑 ...
  }
}
```

---

## 四、API端点补充

### 4.1 需要确认后端已实现的接口

请确认以下后端接口已实现：

```python
# spug_api/apps/document/urls.py
path('check_uploaded_chunks/', CheckUploadedChunksView.as_view()),  # ✅ 已实现

# 需要确认是否已添加
path('transfers/<int:transfer_id>/update_hash/', TransferHashUpdateView.as_view()),  # ✅ 已实现
```

### 4.2 前端API配置

**文件**: `spug_web/src/pages/document/stores/constants/api.js`

```javascript
export const API_ENDPOINTS = {
  // ... 其他端点
  
  // 断点续传
  CHECK_UPLOADED_CHUNKS: `${BASE_URL}/check_uploaded_chunks/`,
  
  // 更新传输哈希（断点续传需要）
  TRANSFER_UPDATE_HASH: (id) => `${BASE_URL}/transfers/${id}/update_hash/`,
}
```

---

## 五、测试验证方案

### 5.1 功能测试用例

| 用例编号 | 测试场景 | 预期结果 |
|---------|---------|---------|
| TC-01 | 上传100MB文件到30%时刷新页面 | 恢复后从30%继续上传，只需上传剩余70MB |
| TC-02 | 上传500MB文件时关闭浏览器，重新打开 | 显示传输记录，点击继续后检查分片并从断点上传 |
| TC-03 | 网络中断5分钟后恢复 | 自动/手动恢复后从断点继续 |
| TC-04 | 同时上传3个大文件，暂停其中一个 | 被暂停文件恢复后从断点继续，不影响其他文件 |
| TC-05 | 分片上传过程中服务器重启 | 恢复后检查分片，只上传缺失的分片 |
| TC-06 | 小文件(<20MB)断点续传 | 正常恢复，不需要重新选择文件 |

### 5.2 性能测试用例

| 用例编号 | 测试场景 | 性能指标 |
|---------|---------|---------|
| PT-01 | 检查1000个分片的状态 | 响应时间 < 2秒 |
| PT-02 | 从50%恢复10GB文件上传 | 总时间 ≈ 50%上传时间 + 5秒检查时间 |
| PT-03 | 并发10个大文件断点续传 | 各文件恢复互不干扰 |

---

## 六、实施计划

### 6.1 实施优先级

```
第一阶段（核心功能）:
  ✅ 1. 添加 checkUploadedChunks API调用
  ✅ 2. 修改 resumeItem 方法支持断点续传
  
第二阶段（用户体验）:
  ⏳ 3. 添加文件重新选择功能
  ⏳ 4. 优化恢复时的提示信息
  
第三阶段（完善）:
  ⏳ 5. 添加断点续传状态显示
  ⏳ 6. 支持自动恢复未完成的任务
```

### 6.2 代码修改清单

| 文件路径 | 修改类型 | 修改内容 |
|---------|---------|---------|
| `stores/constants/api.js` | 新增 | 添加 CHECK_UPLOADED_CHUNKS 端点 |
| `stores/upload/core/transfer.js` | 新增 | 添加 checkUploadedChunks 方法 |
| `stores/upload/core/index.js` | 修改 | 重写 resumeItem 方法 |
| `stores/upload/core/index.js` | 新增 | 添加 resumeChunkedUpload 方法 |
| `stores/upload/core/chunkUpload.js` | 修改 | 支持从指定分片开始 |
| `UploadPanel.js` | 新增 | 添加文件重新选择UI |

---

## 七、风险评估

| 风险项 | 影响 | 缓解措施 |
|-------|------|---------|
| 文件哈希计算耗时 | 大文件恢复时卡顿 | 添加loading提示，使用Worker线程 |
| 分片检查失败 | 从头开始上传 | 优雅降级，不影响正常上传 |
| 文件被修改 | 续传错误数据 | 校验文件大小和修改时间 |
| 存储空间不足 | 续传失败 | 提前检查磁盘空间 |

---

## 八、附录

### 8.1 相关代码引用

**后端断点续传检查视图:**
- 文件: `spug_api/apps/document/views/upload/resume.py`
- 类: `CheckUploadedChunksView`

**前端传输记录恢复:**
- 文件: `spug_web/src/pages/document/Explorer.js:147-247`
- 方法: `restoreTransfers()`

**前端恢复上传:**
- 文件: `spug_web/src/pages/document/stores/upload/core/index.js:519-568`
- 方法: `resumeItem()`

### 8.2 参考文档

- 后端接口文档: `docs/API.md`
- 前端Store架构: `stores/upload/README.md`

---

**文档版本**: v1.0  
**创建日期**: 2026-03-16  
**状态**: 待实施
